# 생산현황 분석 서비스 PRD

- 문서 버전: 1.0
- 작성 기준일: 2026-09-16
- 서비스 가칭: Production Analytics
- 기준 UI: `검사 DATA 분석 서비스 — AI UI/UX 화면 설계서 V28`
- 생산 데이터 원천: `성형작업일보 데이터(수정).xls`
- 참고 자료: 기존 `prd.md`, `26년 4월 생산현황(G,S).xlsx`
- 주요 사용자: 생산관리자, 현장관리자, 설비관리자, 품질관리자

---

## 1. 문서 목적

성형작업일보를 업로드하면 유효 데이터를 검증·정제하고, 생산량과 생산성뿐 아니라 설비 가동률, 비가동, MTTR, 참고 MTBF를 공장·제품유형·설비·품번·작업자·금형 기준으로 분석하는 웹 서비스를 정의한다.

기존 검사 DATA 분석 화면의 글로벌 레이아웃, 필터 카드, KPI 카드, 차트 카드, 검색·정렬·페이지네이션, 상세 이동, 조회상태 유지, 라이트·다크 테마, 반응형 구조를 최대한 재사용한다. 다만 검사 업무에 특화된 품질 지표와 메뉴는 생산 및 설비 지표로 교체한다.

이 문서는 화면, 계산식, 예외 처리, API, 데이터 모델, 상태 문구와 인수 기준까지 포함하며 개발·디자인·QA의 공통 기준으로 사용한다.

---

## 2. 목표와 비목표

### 2.1 목표

1. 생산량, 불량수량, UPH를 동일한 정제 기준으로 제공한다.
2. 작업시간과 비가동시간을 이용해 유효 가동시간과 설비 가동률을 제공한다.
3. `설비이상` 이력을 이용해 고장 건수, MTTR, 참고 MTBF를 제공한다.
4. GROMMET과 SEAL을 동일한 화면 구조에서 분리·비교한다.
5. 대시보드에서 문제 설비를 발견하고 설비 상세, 비가동 이력, 원본 DATA까지 추적할 수 있게 한다.
6. 목록과 상세 화면 사이를 이동해도 조회기간과 필터, 검색, 정렬, 페이지를 유지한다.
7. 오류 행을 정상 데이터와 분리하고, 제외 사유를 사용자가 확인할 수 있게 한다.

### 2.2 현재 버전의 비목표

- 생산계획 대비 달성률
- 표준 C/T 또는 목표 UPH 대비 성능률
- 정확한 이벤트 간 MTBF
- OEE의 성능·품질 구성요소를 포함한 완전한 OEE
- 단가, 생산금액, GRADE, 1SHOT 중량, 원재료 사용량 및 원재료 비용
- 검사 DATA와 결합한 검사 부적합률 분석
- 설비 센서의 실시간 상태 모니터링

---

## 3. 원천 데이터 분석 결과

### 3.1 원천 컬럼

| 원천 컬럼 | 내부 필드 | 유형 | 용도 |
|---|---|---|---|
| 공장 | `factory` | string | 본사/2공장 분석 |
| 작업일자 | `workDate` | date | 기간 집계 |
| 설비명 | `equipmentName` | string | 설비 분석 키 |
| 제품유형 | `productType` | enum | GROMMET/SEAL |
| 품번 | `partNumber` | string | 품번 분석 키 |
| Cavity | `cavity` | integer | 생산 조건 및 상세 정보 |
| 작업판수 | `shotCount` | integer | 생산 활동량 |
| 불량수량 | `defectQuantity` | integer | 생산 불량수량 |
| 실적수량 | `productionQuantity` | integer | 생산량 |
| 작업자 | `operatorName` | string | 작업자 분석 키 |
| 구분 | `shiftType` | enum | 주간/야간 |
| 금형번호 | `moldNumber` | string | 금형 분석 키 |
| 시작시간 | `startedAt` | datetime | 작업 상세·정렬 |
| 종료시간 | `endedAt` | datetime | 작업 상세·정렬 |
| 작업시간(분) | `elapsedMinutes` | integer | 비가동 포함 총 작업시간 |
| 비가동시간(분) | `downtimeMinutes` | integer | 비가동 및 가동률 |
| 비가동내역 | `downtimeReasonRaw` | string | 비가동 사유·고장 후보 |
| 평균샷 | `averageShot` | decimal | 참고값, 현재 핵심 KPI 아님 |

### 3.2 수정본 데이터 프로파일

현재 첨부 수정본의 검증 결과다. 이 값은 개발 로직의 예시이며 운영 데이터에 하드코딩하지 않는다.

| 항목 | 건수 |
|---|---:|
| 전체 행 | 2,208 |
| 작업시간 0분 | 39 |
| 실적수량 0 | 119 |
| 작업시간 0분과 실적수량 0 동시 해당 | 30 |
| 위 두 조건의 합집합 제외 | 128 |
| `비가동시간 > 작업시간` | 30 |
| 계산 가동시간 0분 | 1 |
| 최종 분석 대상 | 2,049 |
| 최종 전체 제외 | 159 |
| 정제 후 고장 후보 | 26 |
| 정제 후 MTTR 산출 대상 | 25 |

정제 후 공장·제품유형별 행 수는 본사 GROMMET 385건, 본사 SEAL 597건, 구지공장 GROMMET 1,057건, 구지공장 SEAL 10건이다.

### 3.3 공장 정규화

원천의 `구지공장`은 저장·표시·필터에서 `2공장`으로 정규화한다. 원본값은 `factoryRaw`에 별도로 보존한다.

| 원본 | 표준값 |
|---|---|
| 본사 | 본사 |
| 구지공장 | 2공장 |

### 3.4 제품유형 정규화

- 앞뒤 공백 제거 후 대문자로 변환한다.
- 현재 허용값은 `GROMMET`, `SEAL`이다.
- 이외 값, 빈 값, `#N/A`, `N/A`, `#NA`는 오류로 분류하고 전체 분석에서 제외한다.
- 설비의 제품유형을 고정 속성으로 간주하지 않는다. 동일 설비가 기간에 따라 GROMMET과 SEAL을 모두 생산할 수 있으므로 각 작업행의 `제품유형`을 사용한다.

---

## 4. 데이터 처리 및 제외 기준

### 4.1 처리 순서

각 행은 아래 순서로 검증한다. 앞 단계에서 오류가 확정되어도 가능한 모든 오류 코드를 수집하여 오류 화면에서 함께 보여준다.

1. 헤더 탐지와 컬럼 매핑
2. 필수값 및 자료형 검증
3. 표준값 정규화
4. 중복 검증
5. 기본 제외조건 검증
6. 가동시간 계산과 시간 논리 검증
7. 고장 후보 및 MTTR 포함 여부 파생
8. 정상 데이터 저장 및 집계

### 4.2 전체 분석 제외 조건

아래 행은 생산량과 불량수량을 포함한 모든 KPI, 차트, 목록 집계, 상세 집계, Excel 다운로드의 분석 데이터에서 제외한다. 오류 화면과 오류 다운로드에서만 조회할 수 있다.

| 오류 코드 | 조건 | 사용자 표시 문구 |
|---|---|---|
| `WORK_TIME_ZERO` | `작업시간(분) = 0` | 작업시간이 0분입니다. |
| `PRODUCTION_ZERO` | `실적수량 = 0` | 실적수량이 0입니다. |
| `DOWNTIME_GT_WORK_TIME` | `비가동시간 > 작업시간` | 비가동시간이 작업시간보다 큽니다. |
| `OPERATING_TIME_NON_POSITIVE` | `작업시간 - 비가동시간 <= 0` | 계산된 가동시간이 0분 이하입니다. |
| `REQUIRED_VALUE_MISSING` | 공장·작업일자·설비명·제품유형·품번·실적수량·작업시간·비가동시간 중 필수값 누락 | 필수값이 누락되었습니다. |
| `INVALID_DATE` | 작업일자 또는 시작시간을 변환할 수 없음 | 날짜 또는 시간이 올바르지 않습니다. |
| `INVALID_NUMBER` | 수량·시간·Cavity·작업판수를 숫자로 변환할 수 없음 | 숫자 형식이 올바르지 않습니다. |
| `NEGATIVE_VALUE` | 실적수량·불량수량·작업시간·비가동시간 중 음수 | 음수 값은 사용할 수 없습니다. |
| `INVALID_PRODUCT_TYPE` | 제품유형이 GROMMET/SEAL이 아님 | 지원하지 않는 제품유형입니다. |
| `NA_PLACEHOLDER` | 매핑 필드에 `#N/A` 계열 문자열 포함 | #N/A 값이 포함되어 있습니다. |
| `DUPLICATE_RECORD` | 동일 중복키의 두 번째 이후 행 | 중복 DATA입니다. |

중복키는 다음 필드의 조합으로 한다.

```text
공장 + 작업일자 + 설비명 + 제품유형 + 품번 + 금형번호
+ 작업자 + 구분 + 시작시간 + 종료시간 + 실적수량
```

중복 시 첫 번째 행만 후보로 유지하고 이후 행은 오류로 제외한다. 첫 번째 행도 다른 제외조건에 해당하면 정상 데이터가 되지 않는다.

### 4.3 가동시간

```text
가동시간(분) = 작업시간(분) - 비가동시간(분)
```

- `작업시간(분)`은 비가동시간을 포함한 총 작업시간으로 정의한다.
- `비가동시간 > 작업시간`이면 계산하지 않고 오류 처리한다.
- 계산값이 0분 이하이면 전체 분석에서 제외한다.
- 원천값은 변경하지 않는다. 계산값은 `operatingMinutes`에 저장한다.
- 음수 가동시간을 0으로 보정하거나 절댓값으로 바꾸지 않는다.

### 4.4 오류 행의 집계 원칙

- 오류 행의 `실적수량`과 `불량수량`이 정상처럼 보여도 합계에 포함하지 않는다.
- 오류 행에 `설비이상`이 있어도 고장 건수, MTTR, 참고 MTBF에 포함하지 않는다.
- 정상 데이터 수와 오류 데이터 수는 별도의 품질 요약으로 표시한다.
- 업로드 결과에서 오류가 1건 이상이면 기본 저장을 차단한다.
- 사용자가 `오류 행 제외하고 반영`을 선택하면 정상 행만 활성 데이터셋으로 저장한다.

### 4.5 경고 조건

경고 행은 분석에 포함하되 업로드 결과에서 확인하도록 한다.

| 경고 코드 | 조건 |
|---|---|
| `MISSING_END_TIME` | 종료시간 누락. 기간 집계는 가능하지만 시간순 원본 상세에 경고 표시 |
| `MISSING_OPERATOR` | 작업자 누락 |
| `MISSING_MOLD` | 금형번호 누락 |
| `DEFECT_GT_PRODUCTION` | 불량수량이 실적수량보다 큼 |
| `SHOT_QUANTITY_MISMATCH` | `Cavity × 작업판수`와 실적수량+불량수량이 다름. 현재 계산 제외가 아닌 확인용 경고 |

### 4.6 데이터 계보

모든 정상·오류 행에 다음을 보존한다.

- 업로드 배치 ID
- 원본 파일명
- 원본 시트명
- 원본 행 번호
- 업로드 시각
- 원본값 JSON
- 정규화값 JSON
- 오류·경고 코드 배열
- 분석 포함 여부

---

## 5. 핵심 지표 계산 기준

모든 집계는 동일 필터와 동일한 유효 데이터 집합을 사용한다. 비율과 평균은 행별 값을 단순 평균하지 않고 합계 기반으로 계산한다.

### 5.1 생산량

```text
생산량(EA) = SUM(실적수량)
```

- 유효 데이터만 합산한다.
- 화면 표기: 천 단위 구분 정수 + `EA`.

### 5.2 불량수량과 생산불량률

```text
불량수량(EA) = SUM(불량수량)
생산불량률(%) = SUM(불량수량) / (SUM(실적수량) + SUM(불량수량)) × 100
```

- 분모가 0이면 `-`로 표시한다.
- 생산불량률은 보조 지표로 제공하며 검사 DATA의 부적합률과 동일 개념으로 취급하지 않는다.

### 5.3 작업시간, 비가동시간, 가동시간

```text
총 작업시간 = SUM(작업시간)
총 비가동시간 = SUM(비가동시간)
총 가동시간 = SUM(작업시간 - 비가동시간)
```

- 저장 단위는 분이다.
- KPI와 표에서는 `시간 분` 형식 또는 소수 1자리 시간으로 표시한다.
- Excel 다운로드에는 원본 분과 변환 시간 값을 모두 제공한다.

### 5.4 설비 가동률

```text
가동률(%) = SUM(가동시간) / SUM(작업시간) × 100
          = (SUM(작업시간) - SUM(비가동시간)) / SUM(작업시간) × 100
```

- 행별 가동률의 평균을 사용하지 않는다.
- 총 작업시간이 0이면 `-`로 표시한다.
- 0.0%~100.0% 범위를 벗어나는 결과가 발생하면 계산 오류로 기록한다.
- 현재 지표는 작업일보에 기록된 작업구간 기준 가동률이다. 계획 가능한 전체 설비시간 기준 가동률과 구분한다.

### 5.5 UPH

```text
UPH(EA/h) = SUM(실적수량) / SUM(작업시간 분) × 60
```

- 기존 PRD와 동일하게 비가동을 포함한 총 작업시간을 분모로 사용한다.
- 행별 UPH나 `평균샷`의 단순 평균을 사용하지 않는다.
- 총 작업시간이 0이면 `-`로 표시한다.
- `순가동 UPH = 생산량 / 가동시간 × 60`은 현재 핵심 KPI에 포함하지 않는다. 필요 시 향후 보조 지표로 추가한다.

### 5.6 고장 건수

```text
고장 후보 = 정상 데이터 AND 비가동내역 토큰에 '설비이상' 포함
고장 건수 = COUNT(고장 후보 행)
```

- 쉼표로 구분된 복합 사유에서도 `설비이상`이 포함되면 고장 1건으로 센다.
- 한 행에 문자열이 여러 번 있어도 1건이다.
- 오류 데이터의 설비이상은 포함하지 않는다.

### 5.7 MTTR

MTTR은 고장 후보 중 단일 사유로 수리시간을 귀속할 수 있는 행만 사용한다.

```text
MTTR 대상 = 고장 후보 AND 비가동내역이 단일 '설비이상'
MTTR(분/건) = SUM(MTTR 대상 비가동시간) / COUNT(MTTR 대상)
```

- `금형교체, 설비이상` 같은 복합 사유는 고장 건수에는 포함하지만 MTTR의 분자와 분모에서 모두 제외한다.
- 화면에는 `고장 26건 · MTTR 산출 25건`처럼 차이가 있으면 보조 문구를 표시한다.
- MTTR 대상 건수가 0이면 `-`와 `산출 가능한 단일 설비이상 이력이 없습니다.`를 표시한다.
- 분 단위 정수 또는 소수 1자리로 표시한다. 60분 이상은 `2시간 15분` 툴팁을 함께 제공한다.

### 5.8 참고 MTBF

현재 데이터에는 정확한 고장 발생·복구 시각이 없으므로 이벤트 간 MTBF를 계산하지 않는다.

```text
참고 MTBF(시간/건)
= 설비별 유효 가동시간 합계(분) / 해당 설비의 고장 건수 / 60
```

- 반드시 설비를 계산 단위로 한다.
- 여러 설비를 합친 전체·공장·제품유형 참고 MTBF는 `해당 범위의 설비별 유효 가동시간 합계 / 해당 범위의 고장 건수`로 계산한다.
- 고장 건수에는 복합 사유 고장도 포함한다.
- 고장 건수가 0이면 `-`로 표시한다. `무고장` 또는 무한대로 표시하지 않는다.
- 명칭은 모든 화면에서 `참고 MTBF`로 표시하고 정보 아이콘에 한계를 설명한다.
- 툴팁: `고장·복구 시각이 없어 유효 가동시간을 고장 건수로 나눈 참고 지표입니다.`
- 향후 고장 발생·복구 시각을 확보하면 이벤트 간 가동시간 평균 방식으로 교체하고 계산 버전을 기록한다.

### 5.9 이전 기간 대비

- 현재 선택기간과 같은 일수의 직전 구간을 비교한다.
- 예: 9월 8일~14일 선택 시 비교기간은 9월 1일~7일이다.
- 생산량·불량수량·고장 건수: 증감률(%). 이전 값 0이면 증감률 대신 `이전 기간 0` 표시.
- 가동률: 퍼센트포인트 차이.
- UPH: 증감률(%).
- MTTR·참고 MTBF: 시간 또는 분 차이와 방향을 표시하되 좋음/나쁨 색상은 MTTR은 감소가 긍정, 참고 MTBF는 증가가 긍정이다.

---

## 6. 전체 메뉴 구조 및 라우트

### 6.1 상단 주요 메뉴

| 순서 | 메뉴 | 라우트 | 목적 |
|---:|---|---|---|
| 1 | 대시보드 | `/` | 핵심 생산·설비 KPI와 주요 문제 요약 |
| 2 | 생산 분석 | `/production` | 기간·공장·제품유형별 생산 추이와 비교 |
| 3 | 설비 분석 | `/equipment` | 설비별 생산성·가동률·신뢰성 비교 |
| 4 | 비가동 분석 | `/downtime` | 비가동 사유와 고장 이력 분석 |
| 5 | 품번 분석 | `/parts` | 품번별 생산량·불량·UPH 분석 |
| 6 | 작업자 분석 | `/operators` | 작업자별 생산실적·UPH 분석 |
| 7 | 금형 분석 | `/molds` | 금형별 생산·불량·비가동 분석 |

### 6.2 더보기 메뉴

| 메뉴 | 라우트 | 목적 |
|---|---|---|
| 스마트 비교 | `/compare` | 기간·설비·품번 비교 |
| 생산 DATA | `/production-data` | 정상 원본·정제값 조회 |
| 데이터 오류 | `/data-errors` | 전체 제외 행과 제외 사유 조회 |

헤더 우측에는 `데이터 업로드`(`/manage`)와 테마 전환 버튼을 둔다. 현재 경로가 더보기 내부 메뉴면 더보기 버튼도 active 상태로 표시한다.

### 6.3 상세 라우트

| 화면 | 라우트 |
|---|---|
| 설비 상세 | `/equipment/:equipmentId` |
| 품번 상세 | `/parts/:partId` |
| 작업자 상세 | `/operators/:operatorId` |
| 금형 상세 | `/molds/:moldId` |
| 비가동 이벤트 상세 | `/downtime/:eventId` |
| 업로드 결과 | `/manage/:batchId/result` |

---

## 7. 글로벌 레이아웃과 조회조건

### 7.1 글로벌 레이아웃

- 기존 화면의 Hyundai/Hyundacorp 브랜딩, sticky 헤더, pill 내비게이션, `더보기` 드롭다운을 유지한다.
- 헤더 문구는 `검사 DATA 분석`에서 `생산현황 분석`으로 변경한다.
- 라이트 배경 `#F4F7FE`, 다크 배경 `#0B1220`, 카드 `#162033`, 블루 accent를 유지한다.
- 카드의 라운드, 약한 그림자, 160ms 테마 전환을 유지한다.
- `ResponsiveGrid`의 `kpi`, `filters`, `split`, `cards`, `dense` 규칙을 재사용한다.
- 모바일에서는 상단 메뉴를 더보기로 축약하고 필터는 세로 카드로 배치한다.

### 7.2 글로벌 조회조건

분석 그룹과 기간을 한 줄에 몰아넣지 않고 별도 카드로 구성한다.

1. 공장 카드
   - 전체
   - 본사
   - 2공장
2. 제품유형 카드
   - 전체
   - GROMMET
   - SEAL
3. 기간 카드
   - 오늘
   - 최근 7일
   - 이번 달
   - 지난 달
   - 올해
   - 사용자 지정

기본값은 `공장 전체`, `제품유형 전체`, `이번 달`이다. 기간은 시스템의 실제 달력을 기준으로 한다. 데이터 최신일을 오늘로 간주하지 않는다.

### 7.3 화면별 상세 조회조건

상세 조건은 `상세 조회조건` 카드에 배치하며 기본 펼침 상태로 제공한다. 좁은 화면에서는 접을 수 있다.

| 조건 | 컨트롤 | 적용 화면 |
|---|---|---|
| 설비 | 검색 가능한 다중 선택 | 생산·설비·비가동·생산 DATA |
| 품번 | 검색 가능한 다중 선택 | 생산·품번·작업자·금형·생산 DATA |
| 작업자 | 검색 가능한 다중 선택 | 생산·작업자·생산 DATA |
| 구분 | 전체/주간/야간 pill | 전체 분석 화면 |
| 금형번호 | 검색 가능한 다중 선택 | 품번·금형·생산 DATA |
| 비가동 사유 | 전체/금형교체/설비이상/근태변경/금형세척/고무이상/제품이상/복합 | 비가동 분석 |

종속 옵션은 현재 공장·제품유형·기간에 존재하는 값만 반환한다. 선택값이 상위 필터 변경으로 더 이상 유효하지 않으면 제거하고 토스트로 알린다.

> 선택한 공장·제품유형에서 사용할 수 없는 상세 조건을 초기화했습니다.

### 7.4 집계 단위

- 62일 이하: 일별 기본
- 63일 이상: 월별 기본
- 생산 분석과 상세 추이에서는 사용자가 일/주/월을 직접 선택할 수 있다.
- 선택한 기간에 맞지 않는 단위는 비활성화한다. 예: 1일 조회에서 월별 선택.
- 주별은 월요일~일요일을 한 주로 정의하고 구간 양 끝의 불완전 주도 표시한다.

---

## 8. 대시보드

### 8.1 화면 구성

위에서 아래 순서다.

1. 화면 제목, 데이터 기준시각, Excel 다운로드
2. 공장 카드
3. 제품유형 카드
4. 기간 카드
5. 데이터 품질 요약 배너
6. KPI 카드 6개
7. 생산량·가동률 추이
8. GROMMET/SEAL 비교
9. 설비별 가동률과 생산량 비교
10. 비가동 사유 구성
11. 설비 요약 TOP 목록

### 8.2 데이터 품질 배너

```text
분석 2,049건 · 제외 159건
[데이터 오류 보기]
```

- 제외 건수가 0이면 중립색으로 `제외된 데이터가 없습니다.`를 표시한다.
- 제외 건수가 있으면 주황색 테두리를 사용하되 KPI 오류처럼 빨간 경고 화면으로 전체 페이지를 막지 않는다.
- `데이터 오류 보기`는 현재 업로드 배치와 현재 기간을 쿼리로 전달한다.

### 8.3 KPI

| KPI | 값 | 보조 정보 |
|---|---|---|
| 총 생산량 | `#,##0 EA` | 이전 기간 대비 |
| 불량수량 | `#,##0 EA` | 생산불량률 |
| 가동률 | `0.0%` | 가동시간/작업시간 |
| UPH | `#,##0 EA/h` | 이전 기간 대비 |
| MTTR | `0.0분` 또는 `-` | 산출건수/고장건수 |
| 참고 MTBF | `0.0시간` 또는 `-` | 참고 산식 툴팁 |

고장 건수는 MTTR 카드의 보조 배지와 비가동 카드에서 제공한다.

### 8.4 차트

#### 생산량·가동률 추이

- 콤보 차트
- 좌측 Y축: 생산량 막대
- 우측 Y축: 가동률 선
- X축: 자동 집계기간
- 툴팁: 기간, 생산량, 가동률, UPH, 유효 행 수
- 데이터가 없는 날짜는 생산량 0, 가동률 `-`로 처리한다.

#### GROMMET/SEAL 비교

- 그룹 막대: 생산량, 불량수량
- 선: 가동률 또는 UPH 전환
- 공장 필터와 기간 필터를 적용한다.
- 특정 제품유형이 선택되면 해당 유형 한 계열만 표시하고 제목을 `GROMMET 생산·설비 현황`처럼 바꾼다.

#### 설비별 가동률과 생산량

- 가동률 하위 10개 설비의 가로 막대
- 각 막대 우측에 생산량을 보조 라벨로 표시
- 설비 클릭 시 설비 상세로 이동
- 작업시간이 충분하지 않은 설비를 임의로 숨기지 않는다. 필요 시 사용자가 최소 작업시간 필터를 선택할 수 있도록 향후 확장한다.

#### 비가동 사유 구성

- 도넛 차트
- 값: 비가동시간 합계
- 범례: 사유, 시간, 비율
- 복합 사유는 `복합 사유`로 별도 표시하며 임의 배분하지 않는다.
- 조각 클릭 시 비가동 분석으로 이동하면서 사유 필터를 적용한다.

### 8.5 설비 요약 테이블

| 컬럼 | 정렬 | 이동 |
|---|---|---|
| 설비명 | 가능 | 설비 상세 |
| 공장 | 가능 | 필터 적용 |
| 주요 제품유형 | 가능 | — |
| 생산량 | 가능 | — |
| 불량수량 | 가능 | — |
| 작업시간 | 가능 | — |
| 비가동시간 | 가능 | 비가동 분석 |
| 가동률 | 가능 | — |
| UPH | 가능 | — |
| 고장 건수 | 가능 | 비가동 분석 |
| MTTR | 가능 | — |
| 참고 MTBF | 가능 | — |

기본 정렬은 가동률 오름차순이다. 최대 10개만 표시하고 `전체 설비 보기`로 설비 분석에 이동한다.

---

## 9. 생산 분석

### 9.1 목적

기간·공장·제품유형별 생산량, 불량수량, UPH와 가동률의 변화를 확인한다.

### 9.2 화면 구성

1. 글로벌 조회조건
2. 상세 조회조건
3. KPI: 생산량, 불량수량, 생산불량률, UPH, 가동률
4. 지표 전환형 추이 차트
5. 공장×제품유형 비교 차트
6. 일/주/월 집계 테이블

### 9.3 추이 차트

- 상단 전환: 생산량 / 불량수량 / UPH / 가동률
- 생산량·불량수량은 막대, UPH·가동률은 선
- `제품유형 전체`이면 GROMMET과 SEAL 계열을 나눠 표시하고 TOTAL을 점선으로 제공한다.
- TOTAL 비율은 각 유형 비율의 평균이 아니라 전체 합계로 재계산한다.

### 9.4 집계 테이블 컬럼

| 기간 | 공장 | 제품유형 | 생산량 | 불량수량 | 생산불량률 | 작업시간 | 비가동시간 | 가동시간 | 가동률 | UPH | 고장 건수 |
|---|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|

기간 셀 클릭 시 해당 기간을 글로벌 사용자 지정 기간으로 적용하고 원본 생산 DATA로 이동할 수 있다.

---

## 10. 설비 분석

### 10.1 목록 화면

조회조건 아래에 검색·정렬·페이지 크기·Excel 다운로드를 둔다.

검색 대상: 설비명, 공장, 주요 품번.

정렬 옵션:

- 생산량
- 가동률
- 비가동시간
- 고장 건수
- MTTR
- 참고 MTBF
- UPH
- 불량수량

목록 컬럼:

| 설비명 | 공장 | 제품유형 구성 | 생산 품번 수 | 생산량 | 불량수량 | 작업시간 | 비가동시간 | 가동률 | UPH | 고장 건수 | MTTR | 참고 MTBF | 상세 |
|---|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|

`제품유형 구성`은 예: `GROMMET 82% · SEAL 18%`로 표시한다. 생산량 기준 구성비다.

### 10.2 설비 상세

화면 상단에 `설비 분석으로 돌아가기` 배너를 둔다. 구성 순서:

1. 설비 기본정보: 설비명, 공장, 조회기간, 제품유형 구성
2. KPI: 생산량, 가동률, UPH, 비가동시간, 고장 건수, MTTR, 참고 MTBF
3. 생산량·가동률 추이 콤보 차트
4. 비가동 사유 도넛과 비가동 추이
5. 품번별 생산실적 표
6. 고장/비가동 이력 표
7. 원본 작업행 표

품번별 표 컬럼:

| 품번 | 제품유형 | 생산량 | 불량수량 | 작업시간 | 가동시간 | 가동률 | UPH | 금형 수 | 작업자 수 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|

고장/비가동 이력 컬럼:

| 작업일자 | 구분 | 품번 | 작업자 | 시작 | 종료 | 작업시간 | 비가동시간 | 사유 | 고장 여부 | MTTR 포함 여부 |
|---|---|---|---|---|---|---:|---:|---|---|---|

### 10.3 설비 분석 주의사항

- 같은 설비가 두 제품유형을 생산하면 선택한 행의 제품유형으로 나눠 집계한다.
- 설비 전체값은 두 유형의 합계 기반으로 다시 계산한다.
- 고장 0건 설비의 참고 MTBF는 `-`이다.

---

## 11. 비가동 분석

### 11.1 화면 구성

1. 글로벌 조회조건
2. 비가동 사유 필터
3. KPI: 총 비가동시간, 비가동 발생 건수, 고장 건수, MTTR, 참고 MTBF
4. 비가동 사유 구성 도넛
5. 기간별 비가동시간·고장 건수 콤보 차트
6. 비가동시간 TOP 10 설비
7. 비가동 이력 테이블

### 11.2 비가동 사유 처리

- 원본 문자열을 쉼표로 토큰화해 `reasonTokens`에 저장한다.
- 원본 문자열도 보존한다.
- 사유별 발생 건수에는 복합 행을 각 토큰에 포함할 수 있으나, 비가동시간 합계 차트에서는 중복 합산을 방지하기 위해 복합 사유를 별도 범주로 표시한다.
- `설비이상` 토큰 포함 여부가 고장 후보의 기준이다.

### 11.3 비가동 이력 컬럼

| 작업일자 | 공장 | 설비명 | 제품유형 | 품번 | 구분 | 작업자 | 작업시간 | 비가동시간 | 가동시간 | 비가동내역 | 고장 후보 | MTTR 포함 | 상세 |
|---|---|---|---|---|---|---|---:|---:|---:|---|---|---|---|

복합 사유의 `MTTR 포함`은 `제외` 배지로 표시하고 툴팁에 다음 문구를 보여준다.

> 복합 사유는 설비이상 시간만 분리할 수 없어 MTTR 계산에서 제외했습니다.

---

## 12. 품번 분석

### 12.1 목록 화면

- 제품유형 탭: 전체 / GROMMET / SEAL
- 검색: 품번, 금형번호
- 정렬: 생산량, 불량수량, 생산불량률, UPH, 가동률, 비가동시간
- 목록 컬럼:

| 품번 | 제품유형 | 생산 공장 | 설비 수 | 금형 수 | 생산량 | 불량수량 | 생산불량률 | 작업시간 | 가동률 | UPH | 상세 |
|---|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---|

### 12.2 품번 상세

1. `품번 분석으로 돌아가기` 배너
2. KPI: 생산량, 불량수량, 생산불량률, UPH, 가동률, 생산 설비 수
3. 생산량·UPH 추이
4. 설비별 생산량·가동률
5. 금형별 생산실적
6. 작업자별 생산실적
7. 비가동 이력
8. 원본 작업행

설비명을 클릭하면 설비 상세, 금형번호를 클릭하면 금형 상세, 작업자를 클릭하면 작업자 상세로 이동한다. 이동할 때 현재 글로벌 필터와 복귀 경로를 유지한다.

---

## 13. 작업자 분석

### 13.1 목록 화면

검색: 작업자명, 작업 품번.

정렬: 생산량, UPH, 불량수량, 생산불량률, 작업시간, 가동률.

| 작업자 | 공장 | 주/야간 구성 | 작업 품번 수 | 설비 수 | 생산량 | 불량수량 | 생산불량률 | 작업시간 | UPH | 가동률 | 상세 |
|---|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---|

### 13.2 작업자 상세

1. `작업자 분석으로 돌아가기` 배너
2. 작업자·기간·공장·주야간 정보
3. KPI
4. 생산량·UPH 추이
5. 품번별 실적
6. 설비별 실적
7. 작업행 상세

작업자 성과는 작업한 품번과 설비 구성에 영향을 받으므로 단순 순위만으로 인사평가에 사용하지 않는다는 안내를 상세 화면 하단에 표시한다.

---

## 14. 금형 분석

### 14.1 목록 화면

검색: 금형번호, 품번, 설비명.

정렬: 생산량, 불량수량, 생산불량률, UPH, 비가동시간, 사용 횟수.

| 금형번호 | 대표 품번 | 제품유형 | 사용 설비 수 | 작업 건수 | 작업판수 | 생산량 | 불량수량 | 생산불량률 | 비가동시간 | UPH | 상세 |
|---|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---|

### 14.2 금형 상세

1. `금형 분석으로 돌아가기` 배너
2. KPI: 생산량, 불량수량, 생산불량률, 작업판수, 비가동시간, UPH
3. 기간별 생산·불량 추이
4. 설비별 사용 실적
5. 작업자별 사용 실적
6. 비가동 이력
7. 원본 작업행

금형교체 시간은 설비 고장 MTTR에 포함하지 않는다.

---

## 15. 스마트 비교

### 15.1 비교 유형

1. 기간 비교
2. 설비 비교
3. 품번 비교

### 15.2 기간 비교

- 동일 공장·제품유형·상세 필터에 기간 A와 기간 B를 적용한다.
- 지표: 생산량, 불량수량, 생산불량률, 작업시간, 가동률, UPH, 고장 건수, MTTR, 참고 MTBF.
- 표는 `지표 / 기간 A / 기간 B / 차이 / 증감률` 구조다.

### 15.3 설비·품번 비교

- 최대 5개 대상을 선택한다.
- 레이더 차트는 단위가 다른 지표를 왜곡할 수 있으므로 사용하지 않는다.
- 지표별 그룹 막대와 비교 표를 제공한다.
- 비율·평균 지표는 각 대상의 합계 기반으로 재계산한다.

---

## 16. 생산 DATA 및 데이터 오류

### 16.1 생산 DATA

정상 분석 대상만 기본 표시한다.

| 작업일자 | 공장 | 설비명 | 제품유형 | 품번 | Cavity | 작업판수 | 불량수량 | 실적수량 | 작업자 | 구분 | 금형번호 | 시작시간 | 종료시간 | 작업시간 | 비가동시간 | 가동시간 | 비가동내역 | 평균샷 |
|---|---|---|---|---|---:|---:|---:|---:|---|---|---|---|---|---:|---:|---:|---|---:|

- 컬럼 고정: 작업일자, 설비명, 품번.
- 나머지는 가로 스크롤을 허용한다.
- Excel 다운로드는 현재 필터와 전체 검색 결과를 적용한다.

### 16.2 데이터 오류

| 원본 행 | 오류 상태 | 오류 사유 | 작업일자 | 공장 | 설비명 | 제품유형 | 품번 | 실적수량 | 불량수량 | 작업시간 | 비가동시간 | 계산 가동시간 | 원본 보기 |
|---:|---|---|---|---|---|---|---|---:|---:|---:|---:|---:|---|

- 한 행에 오류가 여러 개면 배지를 모두 표시한다.
- 계산 가동시간은 음수도 그대로 표시해 원인을 확인할 수 있게 한다.
- 오류 데이터는 분석 합계에 포함되지 않는다는 고정 안내를 표시한다.

> 이 행은 생산량과 불량수량을 포함한 모든 분석에서 제외되었습니다.

---

## 17. 상세 이동과 메뉴 간 연계

### 17.1 드릴다운 규칙

| 출발 | 클릭 대상 | 도착 | 전달 조건 |
|---|---|---|---|
| 대시보드 설비 표 | 설비명 | 설비 상세 | 글로벌 필터 |
| 대시보드 비가동 도넛 | 사유 | 비가동 분석 | 사유+글로벌 필터 |
| 설비 상세 품번 표 | 품번 | 품번 상세 | 설비+글로벌 필터 |
| 품번 상세 설비 표 | 설비명 | 설비 상세 | 품번+글로벌 필터 |
| 품번 상세 금형 표 | 금형번호 | 금형 상세 | 품번+글로벌 필터 |
| 품번 상세 작업자 표 | 작업자 | 작업자 상세 | 품번+글로벌 필터 |
| 비가동 이력 | 상세 | 이벤트 상세 | 이벤트 ID |
| 모든 상세 | 원본 DATA 보기 | 생산 DATA | 대상 조건+기간 |
| 데이터 품질 배너 | 오류 건수 | 데이터 오류 | 배치+기간 |

### 17.2 돌아가기

- 상세 최상단에 `○○ 분석으로 돌아가기` 배너를 표시한다.
- `from` 쿼리와 저장된 `returnPath`를 사용한다.
- 돌아가기 시 목록의 검색, 정렬, 페이지 번호, 페이지 크기, 추가 필터와 스크롤 위치를 복원한다.
- 직접 URL로 진입해 복귀 정보가 없으면 해당 목록의 1페이지로 이동한다.

---

## 18. 필터 및 조회상태 유지

### 18.1 저장소 키

| 구분 | 키 | 유지 항목 |
|---|---|---|
| 글로벌 필터 | `production-analytics-filters` | 공장, 제품유형, 기간 프리셋, 시작일, 종료일, 설비, 품번, 작업자, 주야간, 금형 |
| 목록 상태 | `production-analytics-page:{screenKey}` | 검색어, 정렬 필드, 방향, 페이지, 페이지 크기, 화면별 추가 필터, 스크롤 위치 |
| 테마 | `production-analytics-color-theme` | light/dark |
| 비교 조건 | `production-analytics-compare` | 비교 유형, 대상, 기간 A/B |

`screenKey`는 `equipment`, `downtime`, `parts`, `operators`, `molds`, `production-data`, `data-errors`를 사용한다.

### 18.2 동작 규칙

1. 값 변경 시 `sessionStorage`에 즉시 저장한다.
2. 새로고침과 상세 복귀 후 복원한다.
3. 검색어, 결과 집합을 바꾸는 필터, 페이지 크기를 변경하면 1페이지로 이동한다.
4. 정렬 변경도 1페이지로 이동한다.
5. 저장된 페이지가 총 페이지보다 크면 마지막 페이지로 보정한다.
6. 데이터 업로드 검증 단계에서는 기존 상태를 유지한다.
7. 새 데이터셋을 최종 활성화한 시점에 글로벌 필터와 모든 목록 상태를 초기화한다.
8. 브라우저 뒤로가기는 상세 진입 직전 상태를 복원한다.
9. 공유 가능한 분석 URL에는 공장, 제품유형, 시작일, 종료일, 핵심 대상 ID를 쿼리로 넣는다. 검색·페이지 같은 개인 탐색상태는 sessionStorage만 사용한다.

---

## 19. 공통 목록 UX

- 검색, 정렬, 정렬 방향, 페이지 크기, Excel 다운로드를 한 카드에 배치한다.
- 페이지 크기: 10 / 20 / 50 / 100.
- 전체 검색 결과를 정렬한 후 페이지네이션한다.
- 하단 중앙에 숫자 페이지 버튼을 둔다.
- 현재 페이지는 채움 색으로 강조한다.
- 모바일에서는 주요 4개 컬럼을 우선 표시하고 나머지는 행 확장 패널에서 제공한다.
- 테이블 헤더는 sticky 처리한다.
- 수량과 시간은 우측 정렬한다.
- 정렬 중에는 기존 표 높이를 유지한 skeleton을 표시한다.

---

## 20. 차트 공통 규칙

1. 같은 의미의 지표는 전 화면에서 같은 색을 사용한다.
   - 생산량: 파랑
   - 불량수량: 빨강
   - 가동률: 초록
   - UPH: 보라
   - 비가동시간: 주황
   - MTTR: 진홍
   - 참고 MTBF: 청록
2. GROMMET과 SEAL 색상은 제품유형 비교 차트에서 고정한다.
3. 비율 Y축은 0~100%를 기본으로 하되 작은 변화 확인이 필요한 상세 화면에서만 자동 범위를 허용하고 축 절단 표시를 한다.
4. 툴팁에는 분자·분모를 함께 보여준다. 예: `가동률 92.4% (가동 1,120분 / 작업 1,212분)`.
5. 데이터가 없는 기간을 선으로 연결하지 않는다.
6. 단일 데이터 포인트이면 점과 값 라벨을 표시한다.
7. 차트 클릭으로 적용된 필터는 상단에 해제 가능한 칩으로 표시한다.
8. Excel/PDF 캡처에서도 범례와 축 숫자가 잘리지 않게 여백을 확보한다.

---

## 21. 업로드 및 검증 화면

### 21.1 처리 흐름

```text
파일 선택
→ 상단 40행 내 헤더 자동 탐지
→ 컬럼 매핑
→ 전체 행 검증
→ 정상/경고/오류 요약
→ 오류 행 다운로드 또는 원본 수정
→ 오류 행 제외하고 반영
→ 데이터셋 활성화
→ 캐시·조회상태 초기화
→ 대시보드 갱신
```

### 21.2 헤더 탐지

아래 핵심 컬럼 중 5개 이상이 같은 행에 있고 `작업일자`, `설비명`, `실적수량`, `작업시간(분)`이 반드시 존재해야 한다.

- 공장
- 작업일자
- 설비명
- 제품유형
- 품번
- 실적수량
- 작업시간(분)
- 비가동시간(분)

대소문자, 앞뒤 공백, 연속 공백, 괄호와 구분기호를 정규화해 매칭한다.

### 21.3 결과 요약

```text
전체 DATA      2,208건
분석 대상      2,049건
경고 DATA          N건
오류·제외 DATA   159건
```

오류 사유별 건수는 중복 카운트를 허용하므로 사유 합계가 오류 행 수보다 클 수 있다. 이 점을 안내한다.

### 21.4 저장 정책

- 오류가 없으면 `정상 DATA 반영` 버튼을 제공한다.
- 오류가 있으면 `오류 행 제외하고 반영` 버튼을 제공한다.
- 버튼 클릭 전 확인 모달에서 전체·정상·제외 건수를 다시 보여준다.
- 활성 데이터셋 교체는 트랜잭션으로 처리한다. 집계 생성이 실패하면 기존 활성 데이터셋을 유지한다.
- 이전 배치는 감사용으로 보존하되 일반 화면에서는 최신 활성 배치만 분석한다.

---

## 22. API 요구사항

### 22.1 공통 쿼리

```typescript
type AnalyticsQuery = {
  startDate: string;             // YYYY-MM-DD, inclusive
  endDate: string;               // YYYY-MM-DD, inclusive
  factories?: ('본사' | '2공장')[];
  productTypes?: ('GROMMET' | 'SEAL')[];
  equipmentIds?: string[];
  partIds?: string[];
  operatorIds?: string[];
  moldIds?: string[];
  shiftTypes?: ('주간' | '야간')[];
  grain?: 'day' | 'week' | 'month';
  timezone?: 'Asia/Seoul';
};
```

### 22.2 공통 응답

```typescript
type AnalyticsResponse<T> = {
  data: T;
  appliedFilters: AnalyticsQuery;
  dataQuality: {
    sourceRows: number;
    validRows: number;
    excludedRows: number;
    warningRows: number;
    activeBatchId: string;
  };
  meta: {
    calculatedAt: string;
    metricVersion: string;
    timezone: 'Asia/Seoul';
  };
};
```

### 22.3 엔드포인트

| Method | Endpoint | 용도 |
|---|---|---|
| POST | `/api/v1/uploads/validate` | 파일 임시 업로드 및 검증 |
| GET | `/api/v1/uploads/:batchId/result` | 검증 요약·오류 목록 |
| POST | `/api/v1/uploads/:batchId/activate` | 오류 제외 후 활성화 |
| GET | `/api/v1/filters/options` | 종속 필터 옵션 |
| GET | `/api/v1/dashboard/summary` | KPI·비교기간·품질 요약 |
| GET | `/api/v1/dashboard/trends` | 대시보드 추이 |
| GET | `/api/v1/production/trends` | 생산 추이 |
| GET | `/api/v1/production/summary-table` | 기간 집계 표 |
| GET | `/api/v1/equipment` | 설비 목록 |
| GET | `/api/v1/equipment/:id` | 설비 상세·KPI |
| GET | `/api/v1/equipment/:id/trends` | 설비 추이 |
| GET | `/api/v1/downtime/summary` | 비가동 KPI·사유 구성 |
| GET | `/api/v1/downtime/events` | 비가동 이력 |
| GET | `/api/v1/parts` | 품번 목록 |
| GET | `/api/v1/parts/:id` | 품번 상세 |
| GET | `/api/v1/operators` | 작업자 목록 |
| GET | `/api/v1/operators/:id` | 작업자 상세 |
| GET | `/api/v1/molds` | 금형 목록 |
| GET | `/api/v1/molds/:id` | 금형 상세 |
| GET | `/api/v1/records` | 정상 생산 DATA |
| GET | `/api/v1/data-errors` | 오류·제외 DATA |
| POST | `/api/v1/compare` | 기간·대상 비교 |
| GET | `/api/v1/exports/:view` | 현재 필터 기준 Excel 다운로드 |

목록 API는 `search`, `sort`, `order`, `page`, `pageSize`를 공통 지원한다. 서버가 필터·검색·정렬 후 페이지네이션한다.

### 22.4 KPI 응답 예시

```json
{
  "productionQuantity": 1250000,
  "defectQuantity": 4200,
  "defectRatePercent": 0.3349,
  "elapsedMinutes": 68200,
  "downtimeMinutes": 5400,
  "operatingMinutes": 62800,
  "utilizationRatePercent": 92.0821,
  "uph": 1099.7067,
  "failureCount": 26,
  "mttrEligibleCount": 25,
  "mttrMinutes": 94.6,
  "referenceMtbfHours": 40.26
}
```

API는 계산 정밀도를 유지하고 화면에서 반올림한다. 반올림된 화면값을 다시 집계에 사용하지 않는다.

---

## 23. 데이터 구조

### 23.1 `upload_batch`

| 필드 | 유형 | 설명 |
|---|---|---|
| id | UUID | 배치 ID |
| original_file_name | varchar | 원본 파일명 |
| status | enum | validating/validated/active/failed/archived |
| source_row_count | integer | 전체 행 수 |
| valid_row_count | integer | 정상 행 수 |
| excluded_row_count | integer | 제외 행 수 |
| warning_row_count | integer | 경고 행 수 |
| uploaded_at | timestamptz | 업로드 시각 |
| activated_at | timestamptz nullable | 활성화 시각 |
| metric_version | varchar | 계산 버전 |

### 23.2 `production_record`

| 필드 | 유형 | 설명 |
|---|---|---|
| id | UUID | 내부 ID |
| batch_id | UUID | 업로드 배치 |
| source_sheet | varchar | 원본 시트 |
| source_row_number | integer | 원본 행 |
| factory_raw | varchar | 원본 공장 |
| factory | enum | 본사/2공장 |
| work_date | date | 작업일자 |
| equipment_id | UUID | 설비 차원 키 |
| product_type | enum | GROMMET/SEAL |
| part_id | UUID | 품번 차원 키 |
| cavity | integer | Cavity |
| shot_count | integer | 작업판수 |
| defect_quantity | bigint | 불량수량 |
| production_quantity | bigint | 실적수량 |
| operator_id | UUID nullable | 작업자 |
| shift_type | enum | 주간/야간 |
| mold_id | UUID nullable | 금형 |
| started_at | timestamp nullable | 시작시간 |
| ended_at | timestamp nullable | 종료시간 |
| elapsed_minutes | integer | 작업시간 |
| downtime_minutes | integer | 비가동시간 |
| operating_minutes | integer | 계산 가동시간 |
| downtime_reason_raw | varchar nullable | 원본 비가동내역 |
| reason_tokens | jsonb | 토큰 배열 |
| is_failure_candidate | boolean | 설비이상 포함 |
| is_mttr_eligible | boolean | 단일 설비이상 |
| average_shot | decimal nullable | 평균샷 |
| is_analysis_eligible | boolean | 분석 포함 여부 |
| raw_payload | jsonb | 원본 행 |

### 23.3 `data_quality_issue`

| 필드 | 유형 | 설명 |
|---|---|---|
| id | UUID | 이슈 ID |
| batch_id | UUID | 배치 |
| record_id | UUID nullable | 파싱된 행 ID |
| source_row_number | integer | 원본 행 |
| severity | enum | error/warning |
| issue_code | varchar | 오류 코드 |
| field_name | varchar nullable | 관련 필드 |
| original_value | text nullable | 원본값 |
| message | text | 사용자 문구 |

### 23.4 차원 테이블

- `dim_equipment`: 표준 설비명, 공장, 활성 여부
- `dim_part`: 표준 품번, 제품유형 기본값. 집계 시 작업행 제품유형 우선
- `dim_operator`: 표준 작업자명, 표시명
- `dim_mold`: 금형번호, 대표 품번

원천 표기 변경을 추적할 수 있도록 각 차원에 alias 테이블 또는 alias JSON을 둔다.

### 23.5 집계 전략

- 일별 설비·제품유형·품번 집계 뷰를 생성한다.
- MTTR 대상 건수와 고장 건수는 별도 컬럼으로 유지한다.
- 참고 MTBF는 최종 필터 범위에서 합계로 계산한다. 사전 계산된 비율의 평균을 사용하지 않는다.
- 활성 배치가 바뀌면 집계 캐시를 새 배치 기준으로 재생성한다.

---

## 24. 화면 문구와 상태 처리

### 24.1 로딩

- KPI: 카드 skeleton
- 차트: 축 영역을 유지한 skeleton
- 표: 헤더 유지 + 8행 skeleton
- 문구: `생산현황을 불러오는 중입니다.`

### 24.2 데이터 없음

| 상황 | 문구 | 액션 |
|---|---|---|
| 기간 내 정상 데이터 없음 | 선택한 조건에 분석 가능한 생산 DATA가 없습니다. | 조회조건 초기화 |
| 필터 조합 결과 없음 | 현재 필터 조합에 해당하는 DATA가 없습니다. | 상세 조건 초기화 |
| 고장 없음 | 선택한 조건에 고장 후보가 없습니다. | — |
| MTTR 산출 불가 | 산출 가능한 단일 설비이상 이력이 없습니다. | 비가동 이력 보기 |
| 참고 MTBF 산출 불가 | 고장 이력이 없어 참고 MTBF를 계산할 수 없습니다. | — |
| 오류 없음 | 제외된 데이터가 없습니다. | — |

### 24.3 오류

| 상황 | 문구 |
|---|---|
| API 실패 | 데이터를 불러오지 못했습니다. 잠시 후 다시 시도해 주세요. |
| 파일 형식 오류 | 지원하지 않는 파일 형식입니다. `.xls` 또는 `.xlsx` 파일을 선택해 주세요. |
| 헤더 탐지 실패 | 헤더 행을 찾지 못했습니다. 작업일자·설비명·실적수량·작업시간 컬럼을 확인해 주세요. |
| 업로드 활성화 실패 | 새 DATA를 반영하지 못했습니다. 기존 DATA는 그대로 유지됩니다. |
| 기간 오류 | 종료일은 시작일보다 빠를 수 없습니다. |
| 최대 기간 초과 | 한 번에 조회할 수 있는 기간을 초과했습니다. 기간을 줄여 주세요. |

### 24.4 성공 및 안내

- `오류 159건을 제외하고 정상 DATA 2,049건을 반영했습니다.`
- `조회조건을 초기화했습니다.`
- `Excel 파일 생성을 시작했습니다.`
- `현재 화면의 조회조건을 복원했습니다.`
- `참고 MTBF는 정확한 이벤트 간 MTBF가 아닙니다.`

### 24.5 숫자 표시

- 수량: `1,234,567 EA`
- 비율: `92.4%`
- UPH: `1,250 EA/h`
- 60분 미만: `35분`
- 60분 이상: `2시간 15분`
- 표 정렬용 원본값은 숫자로 유지한다.
- 값 없음은 `-`, 실제 0은 `0`으로 구분한다.

---

## 25. 기존 검사 화면에서 유지할 UI/UX

1. Hyundai/Hyundacorp 로고와 sticky 글로벌 헤더
2. 화면 폭에 따라 주요 메뉴를 더보기로 이동하는 반응형 pill 내비게이션
3. 현재 메뉴 active pill
4. 공통 콘텐츠 셸과 `ResponsiveGrid`
5. 라이트·다크 테마 및 저장
6. 카드 surface, 라운드, 그림자, 블루 accent
7. 조회조건을 한 줄에 몰아넣지 않는 카드형 필터
8. KPI 카드와 이전 기간 대비 표시
9. 검색·정렬·페이지 크기·Excel 다운로드 공통 바
10. 전체 결과 정렬 후 숫자 페이지네이션
11. 상세 상단의 돌아가기 배너
12. 상세 복귀 시 필터·검색·정렬·페이지 복원
13. 데이터 업로드의 헤더 탐지, 검증 결과, 오류 제외 반영 흐름
14. 차트 클릭을 통한 드릴다운
15. 모바일 카드형 목록 및 가로 스크롤 테이블

---

## 26. 생산현황에서 변경할 UI/UX

| 검사 DATA 분석 | 생산현황 분석 변경 |
|---|---|
| 분석 그룹: 본사(SEAL)/본사(유압+그로멧)/2공장 | 공장과 제품유형을 별도 카드로 분리 |
| 검수량·부적합률·부적합수량·폐기비용 | 생산량·불량수량·가동률·UPH·MTTR·참고 MTBF |
| 품질·검사자·비용 중심 메뉴 | 생산·설비·비가동·품번·작업자·금형 중심 메뉴 |
| 부적합 유형 | 비가동 사유 및 설비이상 |
| ppm | 생산불량률 %, 가동률 % |
| 폐기비용 | 현재 미제공 |
| 검사 DATA 오류 기준 | 작업시간·비가동시간·가동시간 논리 오류 추가 |
| 검사자 효율 | 작업자 생산량·UPH. 인사평가 주의 안내 추가 |
| 설비 품질 상세 | 설비 생산성·가동률·신뢰성 상세 |
| AI 챗봇 상시 런처 | V1에서는 숨김. 생산 질의 모델 구현 후 재사용 |

생산 분석은 공장·제품유형 외에도 설비·품번·작업자·주야간 필터가 필요하므로 기존의 `추가 필터 없음` 원칙을 그대로 적용하지 않는다. 대신 상세 조회조건을 별도 카드로 분리해 복잡도를 관리한다.

---

## 27. 현재 구현 범위

### 27.1 V1 포함

- `.xls`/`.xlsx` 업로드
- 헤더 자동 탐지와 컬럼 매핑
- 정상·경고·오류 검증
- 오류 행 완전 제외와 오류 화면
- 공장·제품유형·기간 및 상세 필터
- 대시보드
- 생산·설비·비가동·품번·작업자·금형 분석
- 상세 페이지와 메뉴 간 드릴다운
- 생산량·불량수량·생산불량률·가동률·UPH
- 고장 건수·MTTR·참고 MTBF
- 스마트 비교
- 정상 생산 DATA와 오류 DATA 조회
- Excel 다운로드
- 조회상태 유지
- 라이트·다크 테마
- 데스크톱·모바일 반응형

### 27.2 V1에서 명시적으로 숨길 항목

- 단가
- 생산금액
- GRADE
- 1SHOT 중량
- 총 사용량(g)
- 원재료 비용
- 총 원재료 비용
- 정확한 이벤트 간 MTBF
- OEE
- AI 생산 분석

빈 카드나 `준비 중` 메뉴를 만들지 않고 기능이 준비된 뒤 노출한다.

---

## 28. 추후 개발 범위

### 28.1 원가·금액

ERP 또는 마스터에서 품번·적용기간 기준 데이터를 연계한다.

- 단가
- 생산금액
- GRADE
- 1SHOT 중량
- 총 사용량(g)
- 원재료 단가
- 원재료 비용
- 총 원재료 비용
- 생산량 대비 원재료 비용
- 제품유형·공장·품번별 원가 추이

마스터는 반드시 적용 시작일·종료일을 가져 과거 재집계가 가능해야 한다.

### 28.2 정확한 MTTR/MTBF

향후 다음 이벤트 필드를 수집한다.

- 고장 이벤트 ID
- 고장 발생시각
- 수리 시작시각
- 복구 완료시각
- 생산 재개시각
- 고장 코드
- 수리 코드
- 계획/비계획 정지 구분
- 담당자

이후 계산:

```text
정확한 MTTR = SUM(복구 완료 - 수리 시작) / 수리 완료 고장 건수
이벤트 간 MTBF = AVG(다음 고장 발생 - 이전 복구 완료 중 실제 가동시간)
```

첫 고장 전과 마지막 고장 후의 우측·좌측 검열 구간은 이벤트 기반 버전에서 별도 정책을 확정한다.

### 28.3 기타 확장

- 생산계획 대비 달성률
- 표준 C/T 및 목표 UPH
- OEE
- 검사 DATA와 생산 DATA 연결
- 실시간 MES/설비 연동
- 이상징후 탐지
- 생산 AI 챗봇
- 주간 생산업무 보고

---

## 29. 권한과 감사

- 조회자: 대시보드와 분석·다운로드 사용
- 업로더: 파일 검증과 활성화 가능
- 관리자: 배치 이력, 이전 배치 복원, 차원 alias 관리 가능
- 업로드, 활성화, 오류 제외 반영, 다운로드를 감사 로그에 기록한다.
- 작업자명은 개인정보 최소화 원칙에 따라 권한이 없는 사용자에게 마스킹할 수 있도록 한다.

---

## 30. 성능 및 비기능 요구사항

- 일반 KPI/차트 API P95 2초 이내
- 목록 API P95 2초 이내
- Excel 다운로드는 비동기 작업으로 처리하고 완료 알림 제공
- 동일 필터 요청 캐시, 활성 배치 변경 시 무효화
- 모든 기간 계산은 `Asia/Seoul`
- 모바일 최소 터치 영역 44px, AI 런처를 재도입하면 56px
- 색상만으로 상태를 구분하지 않고 텍스트·아이콘 병행
- 키보드로 필터, 테이블 정렬, 모달, 더보기 메뉴 조작 가능
- 차트에는 스크린리더용 요약 테이블 제공

---

## 31. 테스트 및 인수 기준

### 31.1 데이터 정제

1. `작업시간 = 0` 행은 모든 집계에서 제외된다.
2. `실적수량 = 0` 행은 모든 집계에서 제외된다.
3. `비가동시간 > 작업시간` 행은 생산량·불량수량을 포함한 모든 집계에서 제외된다.
4. `작업시간 - 비가동시간 = 0` 행은 모든 집계에서 제외된다.
5. 제외 행의 `설비이상`은 고장 건수에도 포함되지 않는다.
6. 수정본 샘플 기준 분석 대상 2,049건, 제외 159건이 재현된다.

### 31.2 계산

1. 생산량은 유효 행의 실적수량 합계와 일치한다.
2. 가동률은 합계 기반이며 행별 가동률 평균과 다를 수 있다.
3. UPH는 생산량/총 작업시간×60과 일치한다.
4. 복합 사유 `금형교체, 설비이상`은 고장 건수에 포함되고 MTTR에서 제외된다.
5. 수정본 샘플 정제 후 고장 후보 26건, MTTR 산출 대상 25건이 재현된다.
6. 고장 0건 범위의 참고 MTBF는 0이 아니라 `-`다.
7. 전체·공장·제품유형·설비 KPI는 같은 필터의 상세 합계와 일치한다.

### 31.3 상태 유지

1. 설비 목록 4페이지에서 상세 진입 후 돌아오면 4페이지와 검색·정렬이 복원된다.
2. 상세에서 품번·금형·작업자 메뉴로 이동해도 글로벌 필터가 유지된다.
3. 새 데이터셋 활성화 후 이전 목록 상태가 초기화된다.
4. 데이터 검증만 하고 활성화하지 않으면 현재 분석 화면 상태가 유지된다.

### 31.4 화면 상태

1. 데이터 없음, 고장 없음, MTTR 산출 불가, API 오류가 서로 다른 문구로 표시된다.
2. 실제 0과 미산출 `-`를 구분한다.
3. 다크 모드에서 차트, 표, 경고 배지의 대비가 유지된다.
4. 모바일에서 KPI, 필터, 표 상세를 사용할 수 있다.

---

## 32. 구현 우선순위

### Sprint 1: 데이터 기반

- 업로드, 헤더 탐지, 매핑
- 정제·오류 코드·배치 활성화
- 핵심 데이터 모델과 집계 뷰
- 필터 옵션 API

### Sprint 2: 대시보드·생산

- 글로벌 레이아웃과 상태 저장
- 대시보드 KPI·차트
- 생산 분석
- 정상 DATA·오류 DATA

### Sprint 3: 설비·비가동

- 설비 목록·상세
- 비가동 분석
- MTTR·참고 MTBF
- 드릴다운

### Sprint 4: 차원 분석·비교

- 품번·작업자·금형 분석
- 스마트 비교
- Excel 다운로드
- 반응형·접근성·성능 최적화

---

## 33. 최종 사용자 흐름

```text
성형작업일보 업로드
→ 헤더 탐지와 데이터 검증
→ 오류 행 확인
→ 오류 행을 완전히 제외하고 정상 DATA 반영
→ 대시보드에서 생산량·가동률·UPH·MTTR·참고 MTBF 확인
→ 가동률 저하 또는 비가동 증가 설비 선택
→ 설비 상세에서 품번·금형·작업자·비가동 이력 확인
→ 원본 작업행과 오류 DATA 추적
→ 기간·설비·품번 비교
→ 개선 대상 결정
```

사용자가 최종적으로 답을 얻어야 하는 질문은 다음과 같다.

- 어느 공장과 제품유형의 생산량이 변했는가?
- 어느 설비의 가동률과 UPH가 낮은가?
- 비가동의 주요 원인은 무엇인가?
- 설비이상은 몇 건이며 수리시간은 어느 정도인가?
- 참고 MTBF가 낮은 설비는 무엇인가?
- 어떤 품번·금형·작업자가 생산성과 불량에 연결되는가?
- 지표에 포함되지 않은 오류 DATA는 몇 건이며 왜 제외되었는가?

