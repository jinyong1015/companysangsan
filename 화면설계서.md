# 생산현황 분석 서비스 — UI/UX 화면설계서 V1.3

제품명(가칭): **Production Analytics**  
기업 브랜딩: **Hyundai / Hyundaecorp**  
기준 문서: `prd.md` V1.3  
대상 해상도: Desktop / Tablet / Mobile  
기준 타임존: `Asia/Seoul`  
작성 기준일: 2026-09-17 (로컬 소스 대조 · 관리자 모드·설정·대시보드 추이 반영)

> **V1.3 변경 요약**
> - 헤더 우측 **설정** 팝업으로 화면 모드(라이트/다크)·관리자 모드를 통합. 직접 테마 토글 제거.
> - 생산 DATA 수정·변경 이력·수정 사유를 **관리자 세션** 기준으로 재정의.
> - 대시보드 생산변동 추이에 **비가동시간(분)** 탭 추가.
> - 비가동 TOP 품번 뷰: 가로 순위 / 막대 차트.
> - MTTR·세션·감사 로그를 현재 구현에 맞게 정정.
>
> **V1.2 변경 요약**
> - 전체 UI·메뉴별 사용 순서, 상세 탐색, 재접속 시 복원 범위를 보강했다.
> - 실제 Excel 생성, 업로드 즉시 활성화, 행 수정 후 재검증 동작을 소스에 맞게 정정했다.
> - 화면별 UPH 계산 차이, URL·상태 복원 한계, 오류 목록의 필터 범위를 명시했다.
> - 부록 B~F에 실행 안내, 메뉴별 사용 매뉴얼, 구현 근거, 후속 점검 절차를 추가했다.
>
> **확인 범위:** `C:/Users/PC/Desktop/vibe coding/companysangsan`의 `src/`를 읽어 대조한 문서다. **현재 동작**은 코드에서 확인한 사항이며, **목표/권장/검수 체크리스트**는 구현 완료 또는 테스트 통과를 뜻하지 않는다.

> **V1.1 변경 요약**  
> - 주 메뉴 순서·라벨 재구성 (`가동률 분석`, 비가동→품번→설비 순서). 금형·생산 DATA·오류 DATA는 더보기 고정.  
> - **스마트 비교(`/compare`) 화면 제거** (KPI 이전 기간 대비 표시만 유지).  
> - 대시보드·비가동 화면 재구성 (종합 가동률 게이지, 비가동 히트맵, TOP 품번 등).  
> - 가동률·비가동은 전역 **조회기간 숨김**, 화면 내 **조회월**로 기간 제어.  
> - 생산 DATA 행 수정 모달, 설비/작업자 상세의 작업 품번 선택(`WorkPartSelect`) 반영.

> V1.0 핵심 원칙 유지: 성형작업일보 기반 생산량·불량수량·가동률·UPH·MTTR·참고 MTBF 분석. `작업시간=0`, `실적수량=0`, `비가동시간>작업시간`, `가동시간<=0`인 행은 생산량과 불량수량을 포함한 모든 분석에서 제외한다.

---

## 1. 문서 목적

본 문서는 `prd.md`의 요구사항을 **현재 구현된 화면·사용자 동작** 기준으로 구체화한다.  
디자이너는 레이아웃과 상태를, 프론트엔드 개발자는 컴포넌트·라우팅·상태 저장을, QA는 화면 인수 조건을 확인할 수 있어야 한다.

이 문서의 숫자 예시는 레이아웃 확인용이다. 실제 화면은 활성 데이터셋(시드 가데이터 또는 업로드 DATA)의 정상 행으로 계산한다.

---

## 2. 서비스 핵심 탐색 흐름

```text
데이터 업로드 (/manage)
  ↓
파일 파싱 · 검증 성공 시 즉시 활성 데이터로 저장
  ↓
오류 행 제외 후 정상 DATA 반영
  ↓
대시보드
  ↓
공장 · 제품유형 · (기간 또는 조회월) 선택
  ↓
생산 / 가동률 / 비가동 / 품번 / 설비 / 작업자 / 금형 분석
  ↓
목록 검색 · 정렬 · TOP 차트
  ↓
상세 Drill-down
  ↓
연관 설비 · 품번 · 금형 · 작업자 이동
  ↓
정상 원본 DATA / 오류 DATA 확인
```

사용자가 문제를 찾는 기본 순서:

```text
현재 생산 상태
→ 변화(추이·TOP/WORST)
→ 가동률 또는 생산성 저하 대상
→ 비가동 원인과 고장 이력
→ 관련 품번·금형·작업자
→ 원본 작업행
```

---

## 3. 정보 구조와 라우트

### 3.1 주요 메뉴 (헤더 pill)

| 순서 | 화면 ID | 메뉴 | 라우트 |
|---:|---|---|---|
| 1 | DASH-01 | 대시보드 | `/` |
| 2 | PROD-01 | 생산 분석 | `/production` |
| 3 | UTIL-01 | 가동률 분석 | `/utilization` |
| 4 | DOWN-01 | 비가동 분석 | `/downtime` |
| 5 | PART-01 | 품번 분석 | `/parts` |
| 6 | EQ-01 | 설비 분석 | `/equipment` |
| 7 | OP-01 | 작업자 분석 | `/operators` |

- 폭이 부족하면 뒤쪽 메뉴부터 `더보기`로 이동한다.  
- 우선 유지: **작업자 분석**. 우선 이동 후보: 설비·품번·비가동.

### 3.2 더보기 고정 메뉴

| 화면 ID | 메뉴 | 라우트 |
|---|---|---|
| MOLD-01 | 금형 분석 | `/molds` |
| DATA-01 | 생산 DATA | `/production-data` |
| ERR-01 | 오류 DATA | `/data-errors` |

> **제거됨:** CMP-01 스마트 비교 (`/compare`) — 메뉴·라우트 없음.  
> KPI 카드의 **이전 기간 대비** 비교 표시는 유지한다.

### 3.3 상세·관리 화면

| 화면 ID | 화면 | 라우트 |
|---|---|---|
| EQ-02 | 설비 상세 | `/equipment/:equipmentId` |
| DOWN-02 | 비가동 이벤트 상세 | `/downtime/:eventId` |
| PART-02 | 품번 상세 | `/parts/:partId` |
| OP-02 | 작업자 상세 | `/operators/:operatorId` |
| MOLD-02 | 금형 상세 | `/molds/:moldId` |
| UP-01 | 데이터 업로드 | `/manage` |
| UP-02 | 업로드 검증 결과 | `/manage/:batchId/result` |

---

## 4. 글로벌 화면 골격

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ [Hyundai] Hyundaecorp / 생산현황 분석                                         │
│ [대시보드][생산][가동률][비가동][품번][설비][작업자][더보기⌄] [업로드][설정]   │
├──────────────────────────────────────────────────────────────────────────────┤
│ (가데이터일 때) DemoDataBanner → 데이터 업로드 안내                           │
├──────────────────────────────────────────────────────────────────────────────┤
│ 콘텐츠 셸 (max 1600px)                                                        │
│ ┌ 공장 카드 ┐ ┌ 제품유형 카드 ┐ ┌ 기간 카드 ※ ┐                             │
│ │전체|본사|2공장│ │전체|GROMMET|SEAL│ │오늘|7일|…|지정│                         │
│ └───────────┘ └──────────────┘ └────────────┘                             │
│                                                                              │
│ 화면별 콘텐츠                                                                │
└──────────────────────────────────────────────────────────────────────────────┘
```

※ **기간 카드 표시 규칙**

| 경로 | 전역 필터 | 조회기간 프리셋 |
|---|---|---|
| `/manage`, `/manage/*` | 숨김 | — |
| `/downtime/:eventId` | 숨김 | — |
| `/utilization`, `/downtime` | 공장·제품유형 표시 | **숨김** (화면 내 조회월 사용) |
| 그 외 분석 화면 | 전부 표시 | 표시 |

### 4.1 Header

- sticky, `z-index: 30`.
- 좌측 로고(`/hyundai-logo.png`) 클릭 시 `/`.
- Desktop(xl↑): `Hyundaecorp` + `생산현황 분석` 텍스트.
- 중앙: pill 내비게이션 + 더보기 드롭다운.
- 우측: `데이터 업로드`(`/manage`), **설정** (`SettingsModal`).
- 설정 팝업 탭: `화면 모드`(라이트/다크) · `관리자 모드`(로그인·로그아웃).
- 관리자 로그인 중이면 설정 버튼에 상태 점(●) 표시.
- 더보기: 넘친 주 메뉴 + 구분 라벨(`메뉴 · Insight` / `Insight`) + 고정 메뉴.

### 4.1A `SettingsModal`

- Esc · 바깥 클릭 · 닫기 버튼으로 닫기.
- 화면 모드: 라이트/다크 카드 선택, 즉시 적용, `production-analytics-color-theme` 저장, 기본 **라이트**, 전환 ~200ms(`theme-transitioning`).
- 관리자 모드: 상태 표시 · 비밀번호 로그인 · 로그아웃. 상세는 `prd.md` §30A.

### 4.2 반응형 메뉴

- `ResizeObserver`로 가용 폭 측정.
- `1440px` 이상에서는 주 메뉴 전부 표시.
- 현재 경로가 더보기 안이면 더보기 pill active.
- 바깥 클릭 · `Esc` · 경로 변경 시 드롭다운 닫기.

### 4.3 콘텐츠 셸

- Desktop 최대 너비: `1600px`.
- 좌우 padding: Desktop 24~32px, Tablet 20px, Mobile 16px.
- 화면 제목·액션: `PageHeader`.

---

## 5. 디자인 토큰

### 5.1 색상

| 용도 | Light | Dark |
|---|---|---|
| App background | `#F4F7FE` | `#0B1220` |
| Card surface | `#FFFFFF` | `#162033` |
| Elevated surface | `#FFFFFF` | `#1D2940` |
| Primary accent | `#3B82F6` | `#60A5FA` |
| Text primary | `#111827` | `#F8FAFC` |
| Text secondary | `#6B7280` / `#64748B` | `#94A3B8` |
| Border | `#E8EEF7` / `#E2E8F0` | `#334155` |
| Success | `#16A34A` | `#4ADE80` |
| Warning | `#D97706` | `#FBBF24` |
| Error | `#DC2626` | `#F87171` |

### 5.2 지표 색상

| 지표 | 색상 |
|---|---|
| 생산량 | `#3B82F6` |
| 불량수량 | `#EF4444` |
| 가동률 | `#22C55E` |
| UPH | `#8B5CF6` |
| 비가동시간 | `#F59E0B` |
| MTTR | `#E11D48` |
| 참고 MTBF | `#14B8A6` |
| GROMMET | `#2563EB` (Dark `#60A5FA`) |
| SEAL | `#F97316` (Dark `#FB923C`) |

### 5.3 형태와 타이포

- 기본 글꼴: Pretendard + Apple SD Gothic Neo / Noto Sans KR.
- 화면 제목: 24px/700, Mobile 20px/700.
- 카드 제목: 16px/700.
- KPI 값: Desktop 28~32px/700, Mobile 24px/700.
- 본문 14px / 보조 12px.
- 카드 radius: 16px. 입력·버튼: pill(999px) 또는 10~12px.
- 테마 전환: 약 200ms (`prefers-reduced-motion` 시 생략).

---

## 6. 반응형 그리드

| variant | 최소 열 너비 | 사용처 |
|---|---:|---|
| `kpi` | 200px | KPI 카드 |
| `filters` | 280px | 공장·제품유형·기간 필터 |
| `split` | 420px | 차트 2열 |
| `cards` | 320px | 비교·상세 카드 |
| `dense` | 220px | 선택 대상 카드 |

- `repeat(auto-fit, minmax(...))` 기반. 고정 6열 KPI 금지.
- Mobile: 카드 1열, KPI는 폭이 되면 2열.

---

## 7. 공통 컴포넌트

### 7.1 `PageHeader`

- 제목 · 설명(또는 대상명) · 기준시각 · 우측 액션(Excel 등).

### 7.2 `GlobalFilterSection`

공장·제품유형·기간을 **별도 filter-card**로 표시 (한 줄 바로 합치지 않음).

```text
공장        [전체] [본사] [2공장]
제품유형    [전체] [GROMMET] [SEAL]
기간        [오늘] [최근 7일] [이번 달] [지난 달] [올해] [사용자 지정]
            선택 기간  YYYY-MM-DD ~ YYYY-MM-DD
```

- 단일 선택 pill.
- 사용자 지정: 시작·종료 date input. 종료 < 시작이면 인라인 오류, 조회 중단.

### 7.3 `DetailFilterCard`

```text
상세 조회조건                              [초기화] [접기]
설비 / 품번 / 작업자 / 금형 / 구분(주·야) / (옵션) 비가동 사유
적용 중: [chip ×] …
```

- MultiSelect(검색·모두선택·해제) + removable chip.
- `초기화`는 상세 조건만. 공장·제품유형·기간 유지.
- 화면별로 일부 축 숨김(예: 품번 목록에서 품번 멀티셀렉트 숨김).

### 7.4 `QueryFilterShell`

가동률·비가동 등 **화면 전용 조회조건** 카드.

- 제목, 활성 개수 뱃지, 초기화, 접기/펼치기, 접힌 요약 chip.

### 7.5 `KpiCard`

- 제목 · 정보 아이콘 · 값 · 이전 기간 대비 · 보조 문구(분자/분모 등).
- MTTR↓·참고 MTBF↑가 긍정. 미산출은 `-`.

### 7.6 `DataQualityBanner`

- 분석/제외 건수 + `오류 DATA 보기` → ERR-01.  
  (현재 일부 화면에서는 미노출일 수 있음. 컴포넌트는 유지.)

### 7.7 `SearchSortBar` / `NumberPagination`

- 검색 debounce 300ms. 검색·정렬·페이지크기 변경 시 1페이지.
- 목록의 Excel은 **해당 버튼에 연결된 조회 결과 전체**를 생성한다(페이지 단위 아님). 생산 분석의 상단 버튼과 표 내부 버튼은 범위가 다르므로 §29를 확인한다.
- 숫자 페이지네이션. Mobile `[‹] n/N [›]` 허용.

### 7.8 `BackBanner`

- 상세 첫 콘텐츠. 현재 구현은 `from` 쿼리를 `navigation.ts`에서 목록 경로로 해석해 복귀한다. 검색·정렬·페이지는 해당 목록의 sessionStorage로 복원하며, 임의 `returnPath`나 스크롤 완전 복원은 보장하지 않는다.

### 7.9 `WorkPartSelect`

- 설비·작업자 상세에서 **작업 품번**을 선택해 KPI·차트를 해당 품번 스코프로 좁힘.
- 품번 상세에서 작업자로 진입 시 `partId` 쿼리로 스코프 고정 가능.

### 7.10 `InfoTooltip`

- hover/focus/tap. 참고 MTBF:  
  > 고장·복구 시각이 없어 유효 가동시간을 고장 건수로 나눈 참고 지표입니다.

### 7.11 `DemoDataBanner`

- 시드(가데이터) 사용 중이며 `/manage`가 아닐 때 상단 안내 + 업로드 링크.

---

## 8. 공통 차트 규칙 (설계 원칙; 전 화면 구현 보장 아님)

- hover/focus 툴팁. 범례로 계열 토글.
- 드릴다운 가능 요소는 pointer cursor.
- 선택 필터가 적용되면 removable chip 권장.
- 데이터 없는 날짜: 생산량 0, 비율·평균은 `-` (0%로 연결 금지).
- 비율·평균 툴팁에 분자·분모 표시.
- 접근성: 제목/`aria-describedby`, 가능하면 `표로 보기`.

---

## 9. 공통 테이블 규칙 (설계 원칙)

- sticky header. 숫자 우측 / 텍스트 좌측.
- 상세 이동 셀: 링크 스타일.
- `-` = 미산출, `0` = 실제 0.
- 시간은 화면 `N시간 M분`, Excel에는 분 값 병행 권장.
- Mobile: 카드형 또는 핵심 컬럼 + 확장.

| 상태 | UI |
|---|---|
| Loading | skeleton |
| Empty | EmptyState + 조건 초기화 |
| Error | 인라인 오류 + 다시 시도 |

---

## 10. 조회상태 유지

### 10.1 전역 필터

키: `production-analytics-filters` (sessionStorage)

- 공장, 제품유형, 기간 프리셋, 시작·종료일
- 설비·품번·작업자·금형, 주/야간, (사유)

### 10.2 목록 상태

키: `production-analytics-page:{screenKey}`

- 검색, 정렬, 페이지, 페이지 크기, 화면별 필터, 스크롤(목표)

### 10.3 기타 저장

| 키 | 용도 |
|---|---|
| `production-analytics-color-theme` | 라이트/다크 |
| `production-analytics-target-minutes` | 목표 가동시간(분) |
| `production-analytics-target-shots-v2` | 목표 작업판수 |
| `production-analytics-data-source` | `demo` \| `uploaded` |
| IndexedDB `production-analytics-db` | 업로드 데이터셋 |

### 10.4 복원 규칙

1. 목록 상태를 sessionStorage에 저장하고 상세 링크의 `from`으로 복귀 메뉴를 전달한다.
2. BackBanner/메뉴 재진입 시 복원. 페이지 초과 시 마지막 페이지 보정.
3. **새 데이터셋 반영 성공 시에만** 전역·목록 초기화.
4. 별도의 검증 전용·승인 대기 단계는 없다. 파싱 실패 시 기존 활성 DATA를 유지한다. 브라우저 저장소 실패 시에는 실제 오류 메시지와 활성 상태를 확인한다.

---

# 화면별 설계

---

## 11. DASH-01 대시보드 `/`

### 11.1 목적

선택 범위의 생산·가동·비가동 상태를 한 화면에서 요약하고, 품번·비가동·가동률 상세로 연결한다.

### 11.2 레이아웃 (현재 구현)

```text
[PageHeader: 대시보드 / Excel]
[공장][제품유형][기간]

[KPI × 3: 총 생산량 | 불량수량 | 가동률]

[전체 종합 현황 게이지]  UtilizationOverviewPanel variant=overall
  GROMMET+SEAL (성능·시간·양품·종합설비효율)
  INJECTION+PRESS (성능·시간)

[일별/월별 생산변동 추이]
  지표 탭: 생산량 | 작업품목(품번) 종류 | 평균 SHOT | 비가동시간(분)
  GROMMET / SEAL 분할 추이 차트

[제품유형 탭] [품번 TOP & WORST 10]  (작업시간·SHOT·비가동)

[설비별 일자 비가동 현황]  DowntimeEquipmentHeatmap (선택 비활성)

[비가동시간 TOP 10 품번]  DowntimeTopPartsChart → /parts/:id?from=home
  뷰: 가로 순위 | 막대 차트
```

### 11.3 KPI

| KPI | 표시 |
|---|---|
| 총 생산량 | EA + 이전 기간 대비 % |
| 불량수량 | EA + 생산불량률 hint |
| 가동률 | % + 이전 기간 pp |

> UPH·MTTR·참고 MTBF KPI는 대시보드에서 제거하고 비가동·설비 화면에서 확인한다.

### 11.4 추이 집계 단위

- 조회 기간 길이에 따라 **일별** 또는 **월별** (`dashboardTrendGrain`).
- 제목에 `일별/월별 생산변동 추이` 반영.

### 11.5 빈 상태

- 정상 데이터 없음: EmptyState + 조회조건 초기화.

---

## 12. PROD-01 생산 분석 `/production`

### 12.1 목적

품번별 생산 종합 실적과 SHOT TOP/WORST를 확인한다.

### 12.2 레이아웃

```text
[PageHeader: 생산 분석 / Excel]
[공장][제품유형][기간]

[제품유형 탭] [ITEM SHOT TOP & WORST 10]
  열: 총 작업시간 | 총 SHOT | 비가동 TOP (각 1~10위 품번·값)

[제품별 생산 종합 실적]
  독립 제품유형 탭 | 품번 검색 | 정렬·페이지 | 컬럼 표시 | 전체화면 | Excel
  품번 링크 → /parts/:id?from=production
```

### 12.3 필터 원칙

- **전역 필터만** 사용. DetailFilterCard·KPI 카드 없음.
- 상단 제품유형 탭 → TOP & WORST만.
- 종합 실적 표는 표 상단 탭으로 **독립** 필터.

### 12.4 종합 실적 컬럼

`NO / 제품유형 / 품번 / 비가동·작업·가동시간(분) / 총 SHOT / 가동·전체 작업시간 기준 평균 SHOT / 작업일수 / 일 평균 판수 / 생산·불량·양품수량 / UPH`

- 기본 정렬: 생산수량 내림차순, 페이지 20.
- 제품유형·품번 sticky left 권장.
- 정상 데이터만 집계 (오류 행 제외).

### 12.5 계산 (요약)

```text
가동시간(분) = 작업시간 합 − 비가동시간 합
가동시간 기준 평균 SHOT = 총 SHOT ÷ (가동시간÷60)
전체 작업시간 기준 평균 SHOT = 총 SHOT ÷ (작업시간÷60)
UPH = 생산수량 ÷ (가동시간÷60)   ※ 가동시간 없으면 -
```

---

## 13. UTIL-01 가동률 분석 `/utilization`

### 13.1 목적

조회월 기준 종합 지표와 날짜×설비 히트맵으로 시간·성능 가동률을 확인한다.

### 13.2 레이아웃

```text
[PageHeader: 가동률 분석 / Excel]
[공장][제품유형]          ← 전역 기간 숨김

[조회조건 QueryFilterShell]
  조회월 | 설비(전체설비 / PRESS / INJECTION)
  근무형태(시간) 또는 교대 구분(성능)

[UtilizationOverviewPanel full]
  전체 종합 게이지 (GROMMET+SEAL / INJECTION+PRESS)
  제품유형별 카드 (GROMMET·SEAL + sparkline)
  설비유형별 카드 (INJECTION·PRESS + sparkline)

[탭: 시간가동률 | 성능가동률]
[목표 기준 카드 + 색상 범례] [설정 변경]
[날짜 × 설비 히트맵]
  셀 클릭 → DetailModal → 설비 상세 / 원본 생산 DATA
  푸터: 설비별 · 호기 평균 · INJECTION · PRESS · 전체
```

### 13.3 게이지

- 스케일 0~100%. 초과값은 100%에 고정. **% 목표선 없음**.
- GROMMET+SEAL: 성능·시간·양품률·종합설비효율.
- INJECTION+PRESS: 성능·시간.

### 13.4 목표 편집

| 탭 | 편집 대상 | 저장 키 |
|---|---|---|
| 시간가동률 | 주간+야간·연장·주간 목표 분 | `production-analytics-target-minutes` |
| 성능가동률 | 제품×설비×교대 목표 판수 | `production-analytics-target-shots-v2` |

- 변경 즉시 반영·저장.
- 범례: 95%↑ / 90~95 / 80~90 / ~80 / 없음.

### 13.5 히트맵·모달

- 행: 날짜(+요일), 열: 설비.
- 합계는 **합계 기준 비율** (행 % 평균 금지).
- 모달: 분자·분모·지표 필드 + `설비 상세 이동` + `원본 생산 DATA 보기`.

### 13.6 화면 상태 키

`utilization`: 조회월, 설비유형, 근무형태/교대, metric(time|performance), 스크롤.

---

## 14. DOWN-01 비가동 분석 `/downtime`

### 14.1 목적

조회월 기준 비가동 사유·고장 이력·설비 신뢰성·품번 영향을 분석한다.

### 14.2 레이아웃

```text
[PageHeader: 비가동 분석] (Excel은 하단 표별 제공)
[공장][제품유형]          ← 전역 기간 숨김

[관리기준 aside]
  MTTR 1,440분 초과 / 참고 MTBF 50시간 미만 → 설비점검 권고

[조회조건] 조회월 | 설비(전체/PRESS/INJECTION)

[KPI × 5]
  총 비가동시간 | 비가동 발생 건수 | 고장 건수 | MTTR | 참고 MTBF(툴팁)

┌ 좌: 비가동 사유 구성(도넛) + 사유별 상세 리스트 ─┐ ┌ 우: 설비별 일자 비가동 히트맵 ─┐
│ DowntimeReasonDetailList                         │ │ DowntimeEquipmentHeatmap       │
│ 사유 뱃지·비중 바·귀속시간·건수·건당 평균        │ │ 제품유형 탭 · 지표 토글         │
│                                                  │ │ 셀 선택 → 하단 테이블 필터·스크롤│
└──────────────────────────────────────────────────┘ └────────────────────────────────┘

[비가동시간 TOP 10 품번]  DowntimeTopPartsChart → 품번 상세

[기간별 비가동 사유 현황]  PeriodReasonSection
  제품유형 탭 · Excel · 전체화면 · 요약 모달

[설비별 비가동·신뢰성 현황]  EquipmentReliabilitySection
  설비명 → EQ-02

[비가동 상세 내역]
  검색·정렬·페이지·Excel · 행 → DOWN-02 · 설비 링크
```

### 14.3 히트맵 (`DowntimeEquipmentHeatmap`)

- 제품유형 탭: 전체 / GROMMET / SEAL.
- 지표: 비가동시간 | 발생 건수 | 비가동률.
- 호버 툴팁, 설비이상(고장) 경고 아이콘, 범례.
- **비가동 화면에서만** 셀 선택 → 칩 필터·상세 테이블 연동.  
  대시보드에서는 `selectable=false`.

### 14.4 사유 상세 (`DowntimeReasonDetailList`)

- 도넛과 연동된 사유별 비중·시간·건수·건당 평균.

### 14.5 TOP 품번 (`DowntimeTopPartsChart`)

- 제품유형 탭, **가로 순위 / 막대 차트** 뷰, TOP10 합계·전체 대비.
- 클릭 → `/parts/:id`.

### 14.6 KPI·고장 정의

- 고장 건수: 정상 행 중 `설비이상` 토큰 포함 (복합 포함).
- MTTR: 고장 후보와 동일 집합의 비가동시간 합 ÷ 건수 (복합 사유 포함).
- 고장 0 → MTTR·참고 MTBF `-`.

### 14.7 화면 상태

`downtime`: 조회월, 설비유형, 히트맵 선택 셀, 검색·정렬·페이지 등.

---

## 15. DOWN-02 비가동 이벤트 상세 `/downtime/:eventId`

### 15.1 표시

- 독립 상세 라우트. **전역 필터 숨김**.
- BackBanner + 필드 그리드.

### 15.2 내용

```text
작업일자 / 공장 / 설비명[링크] / 제품유형 / 품번[링크]
금형[링크] / 작업자[링크] / 구분 / 시작·종료
작업·비가동·가동시간 / 비가동내역 / 고장·MTTR 안내
[원본 DATA 보기] → /production-data
```

- 이벤트 없음: `현재 DATA에서 찾을 수 없는 이력입니다.`

---

## 16. EQ-01 설비 분석 `/equipment`

```text
[PageHeader] [공장][제품유형][기간]
[DetailFilterCard: 설비·품번·구분 — 작업자·금형 숨김]
[SearchSortBar] [테이블] [Pagination]
```

| 컬럼 | 비고 |
|---|---|
| 설비명 | → EQ-02 |
| 공장, 제품유형 구성, 생산 품번 수 | |
| 생산량, 불량, 작업/비가동시간, 가동률, UPH | |
| 고장 건수 | → `/downtime?equipment=` |
| MTTR, 참고 MTBF | |

기본 정렬: 가동률 오름차순. 검색: 설비명·주요 품번.

---

## 17. EQ-02 설비 상세 `/equipment/:equipmentId`

```text
[BackBanner]
[작업 품번 선택 WorkPartSelect]
[KPI: 생산량 · 가동률 · UPH · 비가동시간 · 고장 건수]
[생산량·가동률 추이]
[비가동 사유 도넛 + 비가동 이력]
```

- 품번 선택 시 해당 품번 스코프로 KPI·차트 재계산.
- 이력 행 → DOWN-02. 원본 → DATA-01(+설비).

---

## 18. PART-01 품번 분석 `/parts`

```text
[PageHeader] [공장][제품유형][기간]
[생산 수량 품번 TOP 10]  PartProductionTopChart → PART-02
[DetailFilterCard: 설비·금형·작업자·구분 — 품번 멀티셀렉트 숨김]
[SearchSortBar] [테이블] [Pagination]
```

컬럼: `품번 / 제품유형 / 생산 공장 / 설비 수 / 금형 수 / 생산량 / 불량 / 생산불량률 / 작업시간 / 가동률 / UPH`

---

## 19. PART-02 품번 상세 `/parts/:partId`

```text
[BackBanner] 제목=품번번호
[KPI × 8]
  생산량 · 불량수량 · 생산불량률 · UPH · 가동률 · 비가동시간 · 고장 건수 · 생산 설비 수
[기간별 생산량 추이]
[비가동 사유 도넛 + 비가동 이력(최대 20)]
[설비별 생산량·가동률]
[금형별 실적] [작업자별 실적]
```

- 설비 → EQ-02, 금형 → MOLD-02, 작업자 → OP-02(`&partId=`), 전체 비가동 → DOWN-01.

---

## 20. OP-01 작업자 분석 `/operators`

```text
[PageHeader]
[주의 카드] 단순 순위 평가 주의 안내
[생산 수량 작업자 TOP 10]  OperatorProductionTopChart
[DetailFilterCard: 설비·품번·금형·구분 — 작업자 숨김]
[테이블]
```

컬럼: `작업자 / 공장 / 주·야 구성 / 작업 품번 수 / 설비 수 / 생산량 / 불량 / 불량률 / 작업시간 / UPH / 가동률`

---

## 21. OP-02 작업자 상세 `/operators/:operatorId`

```text
[BackBanner] (품번 스코프 가능)
[WorkPartSelect] ※ 스코프 없을 때
[KPI × 6]
[기간별 작업량·UPH 차트]
[설비별(또는 해당 품번) 실적]
```

---

## 22. MOLD-01 금형 분석 `/molds` (더보기)

```text
[PageHeader] [공장][제품유형][기간]
[DetailFilterCard: 설비·품번·작업자·구분 — 금형 숨김]
[테이블] → MOLD-02
```

컬럼: `금형번호 / 대표 품번 / 제품유형 / 사용 설비 수 / 작업 건수 / 작업판수 / 생산량 / 불량 / 불량률 / 비가동시간`

---

## 23. MOLD-02 금형 상세 `/molds/:moldId`

```text
[BackBanner]
[KPI: 생산량 · 불량수량 · 생산불량률 · 작업판수 · 비가동시간]
[설비별 사용 실적 테이블]
```

---

## 24. ~~CMP-01 스마트 비교~~ (제거)

- `/compare` 라우트·메뉴 **삭제**.
- 기간 A/B 전용 비교 UI는 제공하지 않는다.
- 대시보드·목록 KPI의 **이전 기간 대비**만 유지.

---

## 25. DATA-01 생산 DATA `/production-data` (더보기)

### 25.1 목적

정상 원본·정제값 조회. **행 수정·변경 이력은 관리자 모드에서만** 가능.

### 25.2 레이아웃

```text
[PageHeader]
  일반: [수정(비활성)]
  관리자: [변경 이력] [수정]
[공장][제품유형][기간]
[DetailFilterCard: 설비·품번·작업자·금형·구분]
[안내] 관리자: 행 선택/더블클릭 편집 · 일반: 수정은 관리자만
[SearchSortBar] [테이블] [Pagination]
```

### 25.3 권한

| 동작 | 일반 | 관리자 |
|---|---|---|
| 목록·검색·필터·Excel | ○ | ○ |
| 행 수정 모달 | × (로그인 유도) | ○ |
| 변경 이력 | × | ○ |

### 25.4 편집 (`EditProductionRecordModal`)

선택 행에 대해 다음 필드를 편집할 수 있다. **수정 사유(필수)** 입력 후 저장한다.

- 공장, 작업일자, 설비, 제품유형, 품번, 작업자, 구분, 금형
- Cavity, 작업판수, 불량·실적수량, 평균 SHOT
- 시작/종료, 작업·비가동(분), 비가동내역

저장 시 `PATCH /api/production-data/{id}`로 세션 검증·감사 기록 후, 클라이언트에서 재검증·재집계한다.

### 25.5 링크

- 설비·품번·작업자·금형 셀 → 각 상세 화면.

---

## 26. ERR-01 오류 DATA `/data-errors` (더보기)

```text
[PageHeader]
[배치 요약]
[오류코드 카드 토글 필터]
[SearchSortBar / Excel]
[오류 행 테이블]
[원본 보기 슬라이드 패널]
```

- 오류 행은 모든 분석 KPI·차트·집계에서 제외됨을 안내.
- Empty: 현재 조회 결과가 없으면 빈 상태 안내. 배치 전체 오류 0건 여부와는 별도로 확인한다(부록 C.10).

---

## 27. UP-01 데이터 업로드 `/manage`

- **전역 필터 없음.**
- 샘플 엑셀 다운로드 · 시드(가데이터) 복원.
- Dropzone(드래그/클릭) · 진행 상태.
- 결과 요약: 정상 / 경고 / 오류 / 제외 · 품질 Score.
- 파일 선택 후 파싱·검증·IndexedDB 저장이 성공하면 **즉시 활성 DATA로 교체**한다. 별도 분석 반영 승인 버튼·자동 대시보드 이동은 없으며, 결과를 확인한 뒤 대시보드 링크로 이동한다.
- 업로드 DATA는 같은 브라우저·같은 사이트 주소의 IndexedDB에 유지한다. 시드 복원은 저장된 업로드 DATA를 삭제한다. 저장소 삭제 또는 읽기 실패 시에도 업로드 DATA가 복원되지 않을 수 있다.

---

## 28. UP-02 업로드 검증 결과 `/manage/:batchId/result`

```text
[요약 카드 × 4]
[탭: 정상 / 경고 / 오류] 미리보기(최대 100행)
[업로드로 돌아가기] [대시보드] [오류 DATA]
```

---

## 29. Excel 다운로드 UX (현재 구현)

- 버튼은 실제 `onExcel` 핸들러가 있을 때 노출한다. `xlsx`로 브라우저에서 `.xlsx` 파일을 생성하며 Toast는 생성 시작/실패 안내다.
- 서버 비동기 작업이나 완료 알림 서비스는 아니다. 실제 파일 수신 여부는 브라우저 다운로드 목록에서 확인한다.

| 버튼 위치 | 내보내는 범위 |
|---|---|
| 대시보드 상단 | 현재 조회 데이터의 제품유형·품번별 생산량·불량·작업/비가동시간·UPH |
| 생산 분석 상단 | `summaryRows` 전체. 표의 검색·정렬·표 전용 탭 결과와 구분 |
| 생산 분석 종합 실적 표 내부 | 표의 제품유형·검색·정렬을 반영한 `filteredSorted` 전체. 페이지 제한 없음 |
| 가동률 분석 | 현재 조회월·설비 조건·시간/성능 탭의 히트맵 행렬 |
| 비가동 기간별 사유 / 설비 신뢰성 | 각 표의 조회 결과 |
| 비가동 상세 내역 | 해당 목록의 조회 결과 전체 |
| 품번·설비·작업자·금형 목록 | 필터·검색·정렬을 반영한 집계 목록 전체 |
| 생산 DATA / 오류 DATA | 해당 목록의 필터·검색 결과 전체 |
| 업로드의 샘플 엑셀 | 시드 데이터의 샘플 통합문서 |

- 컬럼 숨김은 화면 표시 설정이며, 생산 종합 실적 내보내기는 코드에 정의된 전체 컬럼을 사용한다.
- 현재 파일명은 `생산DATA.xlsx`, `제품별_생산_종합_실적.xlsx` 등 버튼별로 정의한다. 날짜·시각 접미사 통일은 향후 권장사항이다.

---

## 30. 공통 로딩·빈·오류 (설계 목표)

| 상태 | UI |
|---|---|
| Loading | 헤더 유지 + skeleton |
| Empty | EmptyState + 조건 초기화 |
| API/데이터 오류 | 인라인 메시지 + 재시도 |

---

## 31. Toast와 확인 모달

### Toast

- 업로드 성공/실패, Excel 준비, 필터 자동 해제, 데이터셋 복원 등.

### 확인 모달 (목표 동작)

- 새 데이터셋 활성화
- 진행 중 업로드 이탈
- (제거) 비교 조건 전체 초기화 — 비교 화면 삭제에 따라 해당 없음

현재 업로드 활성화와 시드 복원 핸들러에는 확인 모달이 없다. 위 항목은 목표이며, 사용 매뉴얼에서 현재 제공되는 승인 절차로 안내하지 않는다. 일반 필터 초기화에도 확인 모달이 없다.

---

## 32. 상세 화면 간 이동 규칙

| 출발 | 대상 | 도착 | 유지 조건 |
|---|---|---|---|
| DASH-01 | 비가동 TOP 품번 | PART-02 | `from=home` + 글로벌 |
| PROD-01 | 품번 | PART-02 | `from=production` |
| UTIL-01 | 설비명·모달 | EQ-02 / DATA-01 | 조회월·설비 |
| DOWN-01 | 품번 / 설비 / 행 | PART-02 / EQ-02 / DOWN-02 | 조회월·필터 |
| EQ-01 | 고장 건수 | DOWN-01 | `equipment=` |
| EQ-02 | 비가동 행 | DOWN-02 | 이벤트 ID |
| PART-02 | 설비·금형·작업자 | EQ-02 / MOLD-02 / OP-02 | 품번 스코프 |
| OP-02 | 품번·설비 | PART-02 / EQ-02 | 작업자+글로벌 |
| MOLD-02 | 설비 | EQ-02 | 금형+글로벌 |
| 모든 상세 | 원본 DATA | DATA-01 | 대상+글로벌 |
| Demo/품질 배너 | 업로드·오류 | UP-01 / ERR-01 | — |

- 상세→상세 연쇄는 `detailHref`에서 기존 `from`을 전달한다. 대상 필터는 화면의 클릭 핸들러가 갱신하며 URL만으로 모두 복원되지 않는다.
- BackBanner는 최초 목록 복귀 문구를 기본으로 한다.

---

## 33. URL과 상태 표현

일부 쿼리 사용 예시와 향후 공유 URL 형태(전체 필터 복원을 보장하는 예시가 아님):

```text
/equipment/IN-10?factory=본사&productType=GROMMET&startDate=2026-09-01&endDate=2026-09-16
/parts/NE-CC051?from=production
/operators/OP-01?partId=NE-CC051
/downtime?equipment=PS09-3
```

- URL에 넣을 것: 공장, 제품유형, 시작·종료(또는 조회월), 대상 ID, `from`, `partId` 등.
- URL에 넣지 않음: 검색어, 정렬, 페이지, 스크롤.
- 현재 상세 화면은 주로 `from`, 작업자 상세는 `partId`를 읽는다. 전역 공장·기간·설비 등 모든 쿼리를 해석하는 공통 URL 동기화는 확인되지 않았다. `?equipment=`를 주소에 붙이는 것만으로 필터 적용을 기대하지 않는다. 화면의 링크/조회조건을 사용한다. URL 우선 복원은 향후 목표다.

---

## 34. 모바일 설계

- Header: 로고 · pill/더보기 · 업로드·**설정** 아이콘.
- 필터 카드 1열, pill 가로 스크롤, 상세 필터 기본 접힘.
- KPI 1~2열. 차트 최소 높이 300px, 범례 하단.
- 다컬럼 표: 핵심 컬럼 + 행 확장 또는 가로 스크롤.
- drawer/modal: full-screen sheet 권장.

---

## 35. 접근성 (목표·점진 적용)

- visible label, 아이콘 버튼 `aria-label`.
- focus ring accent 2px. Esc로 드롭다운·모달 닫기.
- 오류는 색+아이콘+문구. 대비 WCAG AA 목표.
- 차트 데이터 테이블/`표로 보기` 점진 적용.

---

## 36. 권한별 화면 차이 (현재 구현)

| 기능 | 일반(비관리자) | 관리자 모드 |
|---|---|---|
| 분석 화면 조회 | ○ | ○ |
| Excel 다운로드 | ○ | ○ |
| 생산 DATA 조회·검색·필터·정렬 | ○ | ○ |
| 생산 DATA 단건 수정·저장(사유 필수) | × | ○ |
| 변경 이력 조회 | × | ○ |
| 일괄 수정·셀 인라인 편집 | × | × (목표) |
| 헤더 우측 | `[업로드] [⚙ 설정]` | 동일 + 설정 ● |

설정 팝업 탭: `화면 모드`(라이트/다크) · `관리자 모드`(로그인·로그아웃). 상세는 `prd.md` §30A.

현재 프로토타입은 설정 팝업·관리자 세션·생산 DATA 단건 수정·로컬 감사 로그를 구현했다. 업로드 역할·작업자명 마스킹·일괄 편집 UI는 추후 확장.

---

## 37. 화면별 상태 저장 키

| 화면 | screenKey | 추가 상태 |
|---|---|---|
| 대시보드 | (로컬 state 위주) | 추이 지표, TOP/히트맵 탭 |
| 생산 분석 | `production` | TOP·종합실적 제품유형 탭, 표시 컬럼 |
| 가동률 분석 | `utilization` | 조회월, 설비유형, 근무/교대, metric |
| 비가동 분석 | `downtime` | 조회월, 설비유형, 히트맵 선택, 검색·정렬 |
| 설비 분석 | `equipment` | 정렬·검색·페이지 |
| 품번 분석 | `parts` | 정렬·검색·페이지 |
| 작업자 분석 | `operators` | 정렬·검색·페이지 |
| 금형 분석 | `molds` | 정렬·검색·페이지 |
| 생산 DATA | `production-data` | 검색·정렬·페이지 저장. 선택 행·편집 행은 로컬 state |
| 오류 DATA | `data-errors` | 검색·정렬·페이지 저장. 오류 코드·원본 패널은 로컬 state |

~~스마트 비교 `compare`~~ — 삭제.

---

## 38. 현재 화면에서 표시하지 않는 기능

메뉴·빈 카드·비활성 버튼으로도 노출하지 않는다.

- 스마트 비교 전용 화면
- 단가·생산금액·GRADE·1SHOT 중량·원재료 사용량/비용
- 생산계획 대비 달성률·표준 C/T·목표 UPH
- 정확한 이벤트 간 MTBF
- 계획·표준 C/T 기반 **완전한 산업표준 OEE** (UTIL-01 종합설비효율과 구분)
- 검사 DATA 결합 부적합률 · 센서 실시간 · AI 챗봇

---

## 39. 화면 검수 체크리스트

### 39.1 공통

- [ ] Sticky 헤더, 활성 메뉴·더보기 상태 정확
- [ ] 주 메뉴 순서: 대시보드→생산→가동률→비가동→품번→설비→작업자
- [ ] 더보기 고정: 금형·생산 DATA·오류 DATA (`/compare` 없음)
- [ ] 설정 → 화면 모드 라이트/다크 · 관리자 로그인/로그아웃
- [ ] 공장·제품유형·기간이 별도 카드 (가동률·비가동은 기간 숨김)
- [ ] 상세 필터 초기화 시 글로벌 유지

### 39.2 대시보드·생산·가동률·비가동

- [ ] DASH KPI 3종 + overall 게이지 + 추이(비가동시간 탭 포함) + TOP&WORST + 비가동 히트맵 + TOP 품번
- [ ] PROD: TOP&WORST + 종합실적(컬럼/전체화면), DetailFilter 없음
- [ ] UTIL: 조회월·게이지·목표 편집·히트맵 모달
- [ ] DOWN: 관리기준·KPI5·사유상세·선택 히트맵·TOP품번·기간별·신뢰성·상세내역
- [ ] 오류 행이 분석 수치에 포함되지 않음
- [ ] 참고 MTBF 툴팁, 미산출 `-`

### 39.3 목록·상세

- [ ] 검색·정렬·페이지크기 → 1페이지
- [ ] BackBanner 복귀 시 목록 상태 유지
- [ ] WorkPartSelect(설비·작업자 상세)
- [ ] 생산 DATA: 비관리자 수정 불가 · 관리자 단건 수정·사유·변경 이력

### 39.4 업로드

- [ ] xls/xlsx, 샘플·시드 복원, IndexedDB 유지
- [ ] 반영 후 대시보드, 오류 DATA 연결

### 39.5 관리자

- [ ] `.env.local` 해시로만 로그인 성공, 번들에 평문 없음
- [ ] 세션 없이 PATCH → 403
- [ ] 로그아웃·세션 만료 후 조회 전용 복귀

---

## 40. 구현 컴포넌트 맵

```text
AppLayout
├─ GlobalHeader (ResponsiveNavigation + Upload + Settings)
├─ DemoDataBanner
├─ ContentShell
│  ├─ PageHeader
│  ├─ GlobalFilterSection
│  ├─ DetailFilterCard / QueryFilterShell
│  ├─ KpiCard / SectionCard / EmptyState
│  ├─ SearchSortBar / NumberPagination / BackBanner
│  └─ WorkPartSelect
├─ SettingsModal (화면 모드 · 관리자 모드)
├─ ToastViewport
└─ Modals (EditProductionRecord, ChangeHistory, Utilization Detail, …)
```

화면 전용:

```text
DashboardPage
├─ UtilizationOverviewPanel (overall)
├─ PeriodProductSplitTrendCharts  (+ 비가동시간 지표)
├─ ProductTypeTabs + ProductShotTopWorst
├─ DowntimeEquipmentHeatmap
└─ DowntimeTopPartsChart

ProductionAnalysisPage
├─ ProductShotTopWorst
└─ ProductPerformanceSummary

UtilizationPage
├─ UtilizationOverviewPanel (full)
├─ Target editors
└─ UtilizationHeatmap + DetailModal

DowntimePage
├─ DowntimeReasonDetailList
├─ DowntimeEquipmentHeatmap (selectable)
├─ DowntimeTopPartsChart
├─ PeriodReasonSection
└─ EquipmentReliabilitySection

PartsPage / OperatorsPage
├─ PartProductionTopChart
└─ OperatorProductionTopChart

ProductionDataPage
├─ EditProductionRecordModal
└─ ChangeHistoryModal

ManagePage / ManageResultPage
├─ Dropzone · ValidationSummary
└─ Preview tabs
```

---

## 41. 최종 UX 원칙

1. **정상 DATA와 오류 DATA를 섞지 않는다.**
2. **비율은 합계 기반** — 행별 % 평균 금지.
3. **미산출(`-`)과 0을 구분한다.**
4. **참고 지표의 한계를 숨기지 않는다** (참고 MTBF).
5. **글로벌 필터와 화면 전용 조회조건을 분리한다** (가동률·비가동=조회월).
6. **탐색 맥락을 보존한다** (목록↔상세).
7. **문제 → 대상 상세 → 이력 → 원본 DATA** 연결을 끊지 않는다.
8. **제공할 수 없는 기능은 노출하지 않는다** (스마트 비교 화면 포함).
9. **UTIL-01 종합설비효율**은 목표 가동시간·목표 판수 기준이며, 계획/표준 C/T 기반 완전 OEE와 구분한다.

---

## 부록 A. 메뉴 ↔ 기능 빠른 참조

| 메뉴 | 사용자가 할 수 있는 일 |
|---|---|
| 대시보드 | KPI·종합 가동률·생산 추이·SHOT TOP/WORST·비가동 히트맵·비가동 TOP 품번 확인 |
| 생산 분석 | 품번 TOP/WORST·종합 실적 표 검색·정렬·컬럼·Excel·품번 상세 이동 |
| 가동률 분석 | 조회월·설비유형으로 게이지/카드/히트맵 확인, 목표 편집, 셀 상세→설비·원본 |
| 비가동 분석 | 사유·히트맵(셀 필터)·TOP 품번·기간별·설비 신뢰성·이벤트 상세 |
| 품번 분석 | TOP10·필터·목록·상세(설비·금형·작업자·비가동) |
| 설비 분석 | 필터·목록·상세(품번 스코프)·고장→비가동 |
| 작업자 분석 | TOP10·주의 안내·목록·상세(품번 스코프) |
| 금형 분석 | 목록·상세(설비 사용 실적) |
| 생산 DATA | 원본 조회 · **관리자** 행 수정·변경 이력 |
| 오류 DATA | 제외 행·오류코드 필터·원본 패널 |
| 데이터 업로드 | 엑셀 업로드·검증·반영·시드 복원 |
| 설정 | 화면 모드 · 관리자 로그인/로그아웃 |

---

---

## 부록 B. 다음 사용을 위한 시작 안내

### B.1 실행·접속

- 프로젝트 위치: `C:/Users/PC/Desktop/vibe coding/companysangsan`.
- 프로젝트 폴더에서 최초 의존성 준비는 `npm install`, 개발 서버 실행은 `npm run dev`를 사용한다. 이미 실행 중이면 기존 서버에 접속한다.
- 기본 접속 주소는 `http://localhost:3000`이며, 실제 포트는 실행 터미널 안내를 확인한다. 배포된 운영 주소는 이번 확인 범위에 없다.
- 처음에는 시드 가데이터를 사용한다. 상단 가데이터 배너와 업로드 화면의 활성 파일명으로 실제 DATA 여부를 먼저 확인한다.
- 정상 데이터 날짜가 현재 조회기간 밖에 있으면 빈 화면이 나올 수 있다. 일반 분석은 전역 기간, 가동률·비가동은 화면 내 조회월을 확인한다.

### B.2 전체 UI 읽는 순서

1. **상단 고정 헤더:** 로고는 대시보드, 가운데 pill은 주 메뉴, 더보기는 금형·생산 DATA·오류 DATA, 오른쪽은 데이터 업로드·**설정**이다.
2. **가데이터 안내 → 전역 필터 → 화면 제목/액션 → 본문:** 실제 공통 셸은 필터를 화면별 제목보다 먼저 배치한다. 앞선 화면별 도식은 구성요소 요약이며 DOM 순서가 아니다.
3. **공장·제품유형·기간:** 다른 메뉴와 공유되는 조회 범위다. 상세 조회조건은 추가로 범위를 좁힌다.
4. **KPI·차트:** 요약과 분포를 확인한다. 제품유형 탭은 해당 패널 전용일 수 있어 상단 전역 필터와 구분한다.
5. **목록 표:** 검색·정렬·페이지 크기를 선택하고 링크 셀로 상세를 연다. 검색·정렬·페이지 크기 변경은 첫 페이지로 돌아간다.
6. **상세 상단 복귀 배너:** 출발 메뉴로 돌아간다. 상세 간 연쇄 이동에서도 `from`을 전달하는 링크는 최초 출발 메뉴를 유지한다.
7. **설정:** 화면 모드(라이트/다크)와 관리자 모드를 여기서 전환한다. 생산 DATA를 수정하려면 관리자 로그인이 필요하다.

### B.3 저장과 복원 범위

| 내용 | 저장 방식 | 다음 사용 시 주의 |
|---|---|---|
| 공장·제품유형·기간·상세 조건 | sessionStorage | 같은 탭 새로고침에서 복원. 새 탭·다른 브라우저·세션 종료 후 동일 조건을 보장하지 않음 |
| 목록 검색·정렬·페이지·일부 전용 조건 | sessionStorage | 화면별 키 사용. 모든 탭·모달·스크롤이 저장되는 것은 아님 |
| 테마·목표 시간·목표 판수 | localStorage | 같은 브라우저 프로필·사이트 주소에서 유지 |
| 업로드 원본/정제 레코드·활성 배치·요약 | IndexedDB | 같은 브라우저·사이트에서 재접속 시 복원. 다른 PC·포트로 자동 공유되지 않음 |
| 업로드 DATA의 행 수정 | IndexedDB | 저장 성공 후 재접속에서도 유지 |
| 가데이터의 행 수정 | 메모리 | 새로고침 후 유지되지 않음 |
| 원본 패널·선택 행·편집 모달 | 컴포넌트 state | 화면 이탈/재마운트 후 유지하지 않음 |

새 파일 반영은 기존 활성 데이터셋에 추가하는 방식이 아니라 **교체**다. 서버 계정 저장·배치 이력 조회·이전 배치 복원은 제공하지 않는다. 시드 복원 후 기존 파일을 다시 사용하려면 재업로드한다. 원본 엑셀은 별도로 보관한다.

---

## 부록 C. 메뉴별 실제 사용 절차

### C.1 대시보드

1. 공장·제품유형·기간을 고른다.
2. 총 생산량·불량수량·가동률 KPI와 전체 종합 게이지를 확인한다.
3. 생산변동 추이에서 생산량/작업품목 종류/평균 SHOT/**비가동시간(분)**을 전환한다. 선택 기간에 따라 일별·월별 집계가 달라진다.
4. 제품유형 탭으로 TOP/WORST를 좁히고, 비가동 히트맵에서 집중 날짜·설비를 찾는다.
5. 비가동 TOP 품번을 눌러 품번 상세로 이동한다. 대시보드의 히트맵 셀은 상세 목록 선택 필터로 작동하지 않는다.

### C.2 생산 분석

1. 전역 조회조건을 정하고 상단 제품유형 탭으로 SHOT TOP/WORST를 확인한다.
2. 아래 종합 실적 표의 제품유형 탭·품번 검색을 별도로 설정한다. 위 순위 패널과 표의 제품유형 탭은 독립적이다.
3. 정렬·페이지 크기·표시 컬럼을 조정하고 필요하면 전체화면으로 표를 확대한다.
4. 품번 링크로 상세를 연다. 검색 결과를 파일로 받으려면 표 내부 Excel 버튼을 사용한다(상단 Excel과 범위 차이: §29).

### C.3 가동률 분석

1. 공장·제품유형과 화면 내 조회월·설비유형을 고른다. 전역 조회기간 카드는 표시하지 않는다.
2. 종합·제품유형·설비유형 게이지를 확인한다.
3. 시간가동률/성능가동률을 전환하고 탭에 맞는 근무형태 또는 교대 조건을 선택한다.
4. 설정 변경에서 목표 시간 또는 제품×설비×교대별 목표 판수를 편집한다. 목표 변경은 지표와 색상에 영향을 주고 로컬 저장된다.
5. 날짜×설비 셀을 눌러 분자·분모를 확인한다. 모달의 설비 상세/원본 DATA 이동을 사용한다.
6. Excel로 현재 행렬을 받는다. 목표가 없는 조합(예: SEAL INJECTION 성능 목표)은 단순 0%와 구분한다.

### C.4 비가동 분석

1. 공장·제품유형·조회월·설비유형을 고른다.
2. 총 시간·발생 건수·고장 건수·MTTR·참고 MTBF와 관리기준 안내를 확인한다.
3. 사유 구성과 사유별 상세 목록을 읽고, 히트맵에서 제품유형/비가동시간·건수·비율을 전환한다.
4. 셀을 선택하면 해당 날짜·설비를 하단 상세 내역에 연결한다. 선택 칩을 해제하거나 조회조건을 초기화해 범위를 넓힌다.
5. TOP 품번, 기간별 사유, 설비 신뢰성 표를 확인한다. 각 표의 Excel/전체화면 기능은 해당 영역에 적용된다.
6. 이벤트 행은 비가동 상세, 설비 링크는 설비 상세로 이동한다.

### C.5 품번 분석·상세

1. 생산 수량 TOP10과 조회조건으로 관심 품번을 찾고 품번 목록을 검색·정렬한다.
2. 품번 상세에서 8개 KPI, 기간별 추이, 비가동 사유·이력, 설비·금형·작업자 실적을 확인한다.
3. 이력은 최대 20행 영역과 전체 비가동 이동을 구분한다.
4. 작업자 실적 링크는 `partId`를 전달할 수 있다. 도착한 작업자 상세가 해당 품번으로 제한되어 있는지 확인한다.

### C.6 설비 분석·상세

1. 설비·품번·주야간 조건 또는 검색으로 대상을 찾는다. 기본은 가동률 오름차순이다.
2. 고장 건수 링크로 비가동 분석을 열거나 설비명으로 상세를 연다.
3. 상세의 작업 품번을 고르면 해당 품번으로 KPI·추이·비가동 이력을 좁힌다.
4. 비가동 이력 → 이벤트 상세 → 원본 DATA 순으로 원인을 추적한다. 도착 화면의 실제 조건을 확인한다.

### C.7 작업자 분석·상세

1. 주의 안내를 읽고 설비·품번·금형·주야간 조건을 맞춘 다음 TOP10·목록을 비교한다.
2. 작업자 상세에서 생산·품질·시간·UPH 지표, 작업량·UPH 추이와 실적을 확인한다.
3. 품번에서 진입했다면 품번 제한 상태를 확인한다. 제한 없는 진입에서는 작업 품번 선택으로 범위를 바꾼다.
4. 작업 품번·설비와 근무 조건 차이가 있으므로 총 생산량만으로 작업자 능력 순위를 판단하는 용도로 해석하지 않는다.

### C.8 금형 분석·상세

1. 더보기 → 금형 분석을 연다.
2. 전역 및 설비·품번·작업자·주야간 조건, 검색·정렬로 금형을 찾는다.
3. 금형 상세에서 생산량·불량·불량률·작업판수·비가동시간과 설비별 사용 실적을 확인한다.
4. 설비 링크로 이동한 뒤 복귀 배너로 출발 목록에 돌아간다.

### C.9 생산 DATA와 행 수정

1. 더보기 → 생산 DATA를 열고 조건·검색으로 정상 분석 대상 행을 찾는다.
2. **관리자가 아니면** 수정 버튼이 비활성이다. 더블클릭/수정 시도 시 설정 → 관리자 로그인을 유도한다.
3. 설정 → 관리자 모드에서 `.env.local`에 설정한 비밀번호로 로그인한다.
4. 행 클릭 후 수정/선택 행 수정, 또는 더블클릭으로 모달을 연다. **수정 사유는 필수**다.
5. 식별정보·일시·수량·시간·비가동내역을 수정한다. 시작/종료 변경은 작업시간을 계산하고, 수량/판수 변경은 수동 입력 상태가 아닐 때 평균 SHOT을 계산한다.
6. 저장하면 서버 세션 검증·감사 기록 후 재검증·요약 재집계되어 전체 메뉴에 반영한다. 오류가 생기는 값도 저장될 수 있으며, 그 행은 정상 목록에서 빠지고 오류 DATA에 나타난다.
7. **변경 이력**에서 최근 수정 로그를 확인한다. 업로드 DATA는 브라우저 저장소에 저장되며, 원래 PC의 엑셀 파일을 직접 덮어쓰지 않는다.

### C.10 오류 DATA

1. 더보기 → 오류 DATA에서 배치 요약·오류 코드 카드를 확인한다.
2. 오류 코드로 좁히고 원본 행 번호·설비·품번을 검색한다. 원본 보기를 눌러 패널에서 원본 값을 읽는다.
3. **오류 코드 카드 건수는 활성 데이터셋 전체 제외 행 기준**, 표는 공장·제품유형·기간·오류 코드·검색 기준이다. 두 건수가 다를 수 있다.
4. 목록이 비어 있어도 배치 전체 오류 0건을 뜻하지 않을 수 있다. 특히 날짜 오류 행은 기간 비교에서 빠질 수 있으므로 업로드 검증 결과의 오류 탭도 확인한다.
5. 오류 DATA는 직접 수정 화면이 아니다. 원본 엑셀을 정정해 재업로드하면 데이터셋 전체가 교체된다. 오류 목록 Excel은 현재 조회 범위를 내보낸다.

### C.11 데이터 업로드·검증 결과

1. 데이터 업로드 → 필요하면 샘플 엑셀 다운로드로 형식을 확인한다.
2. `.xls`/`.xlsx` 파일을 선택하거나 드롭한다. 현재 파서는 첫 번째 워크시트를 읽으므로 분석 대상 시트를 먼저 배치한다.
3. 파싱·헤더 인식·검증·저장이 성공하면 즉시 활성 DATA를 교체하고 조회상태를 초기화한다. 정상 행 날짜 범위가 있으면 전역 기간을 해당 범위로 설정한다.
4. 품질 Score와 정상·경고·오류·제외 수를 확인한다. 분석 대상 수에는 경고가 있는 적격 행도 포함되므로 정상/경고/오류 숫자를 무조건 더하지 않는다.
5. 검증 결과에서 정상(경고 없음)/경고/오류 탭을 확인한다. 각 탭 미리보기는 최대 100행이며 초기 탭은 오류다.
6. 대시보드 링크로 분석을 시작한다. 결과 페이지는 현재 활성 DATA를 사용하며, URL의 과거 `batchId`만으로 이전 업로드 내용을 불러오는 기능은 없다.
7. 시드 데이터로 복원은 현재 업로드 저장 데이터를 삭제하고 가데이터로 전환한다. 현재 구현은 별도 확인 모달 없이 실행된다.

---

## 부록 D. 지표 해석과 현재 구현 차이

| 항목 | 현재 계산/동작 | 해석 시 주의 |
|---|---|---|
| 일반 KPI 가동률 | 유효 가동시간 합 ÷ 작업시간 합 × 100 | 목표 시간 대비인 시간가동률과 분모가 다름 |
| 일반 KPI UPH | 생산수량 합 ÷ 작업시간 합 × 60 (`metrics.ts`) | 생산 종합 실적 표와 동일한 정의가 아님 |
| 생산 종합 실적 UPH | 생산수량 합 ÷ 가동시간 합 × 60 (`aggregates.ts`) | 비가동이 있으면 일반 KPI UPH와 달라짐 |
| 생산 종합 실적 양품수량 | 생산수량 − 불량수량 | 종합 게이지 양품률의 생산수량 해석과 차이가 있으므로 업무 정의 통일이 필요 |
| 생산불량률 | 불량 ÷ (생산 + 불량) × 100 | 단순 불량 ÷ 생산과 다름 |
| 게이지 양품률 | 생산 ÷ (생산 + 불량) | 표의 양품수량을 분자로 쓰는 공식이 아님 |
| 종합설비효율 | 시간가동률 × min(성능가동률, 100%) × 양품률 | 목표 기준 참고 지표. 완전한 산업표준 OEE로 표기하지 않음 |
| 사유별 시간 | 복합 사유이면 각 토큰에 비가동시간 균등 배분 | 실제 사유별 측정시간이 아님 |
| 사유별 건수 | 각 사유 토큰에 각각 1건 | 토큰 건수 합이 비가동 행 수보다 클 수 있음 |
| MTTR | 설비이상 토큰 포함 행(복합 포함)의 전체 비가동시간 ÷ 해당 행 수 | 사유 도넛의 설비이상 귀속시간과 다를 수 있음 |
| 참고 MTBF | 유효 가동시간 합 ÷ 고장 건수 ÷ 60 | 단위 시간. 고장 0건이면 미산출 |
| 원본 평균 SHOT | 편집 모달 자동값은 실적수량 ÷ 작업판수 | 차트의 시간당 SHOT(판수 ÷ 시간)과 다름 |

이 차이는 문서에서 현재 동작을 보존하기 위한 기록이다. 이번 요청에서는 계산 코드를 수정하지 않았다. 지표 정의를 통일할 때는 업무상 실적수량/양품수량의 의미와 UPH 분모를 결정한 뒤 표·KPI·내보내기를 함께 변경한다.

---

## 부록 E. 구현 근거와 유지보수 위치

아래 경로는 프로젝트 루트 기준이다. 문서를 다른 위치로 옮겨도 프로젝트를 찾을 수 있도록 부록 B에 절대 경로를 기록했다.

| 수정하거나 확인할 내용 | 코드 위치 |
|---|---|
| 메뉴 순서·더보기·활성 메뉴 | `src/components/layout/GlobalHeader.tsx` |
| 설정·관리자 UI | `src/components/admin/SettingsModal.tsx`, `src/context/AdminContext.tsx` |
| 관리자 세션·비밀번호·감사 | `src/lib/admin/*`, `src/app/api/admin/*`, `src/app/api/production-data/*` |
| 공통 셸·필터 노출 예외 | `src/components/layout/AppProviders.tsx` |
| 색상·타이포·반응형 CSS | `src/app/globals.css` |
| 필터 UI·전역 상태 | `src/components/filters/FilterCards.tsx`, `src/context/FilterContext.tsx` |
| 테마 | `src/context/ThemeContext.tsx`, `src/app/layout.tsx` (boot script) |
| 메뉴별 본문 | `src/app/page.tsx`, `src/app/<메뉴>/page.tsx` |
| 상세 화면 | `src/app/equipment/[equipmentId]/page.tsx` 등 각 상세 경로 |
| 검색·정렬·페이지 저장 | `src/hooks/usePageState.ts`, `src/lib/storage.ts` |
| 상세 간 이동·출발 메뉴 | `src/lib/navigation.ts` |
| 정상 분석 KPI·비가동 사유 | `src/lib/metrics.ts` |
| 생산 종합 실적 계산 | `src/lib/aggregates.ts` |
| 가동률 목표·히트맵·Excel | `src/lib/utilization.ts`, `src/app/utilization/page.tsx` |
| 생산 표·표 전용 Excel | `src/components/production/ProductPerformanceSummary.tsx` |
| 비가동 표·표 전용 Excel | `src/components/downtime/DowntimeDetailTables.tsx`, `src/lib/downtimeDetail.ts` |
| 엑셀 파싱·샘플·공통 내보내기 | `src/lib/excelParse.ts` |
| 활성 DATA·행 수정·재검증·브라우저 저장 | `src/context/DataSourceContext.tsx`, `src/lib/recordValidate.ts`, `src/lib/idb.ts` |
| 행 편집 입력 UI | `src/components/production-data/EditProductionRecordModal.tsx` |
| 변경 이력 UI | `src/components/admin/ChangeHistoryModal.tsx` |
| 관리자 해시 스크립트 | `scripts/hash-admin-password.mjs` |

---

## 부록 F. 다음 변경 시 재확인할 인수 시나리오

이번 갱신에서는 소스 대조만 수행했다. 아래는 다음 실행 검수 때 사용할 절차이며 아직 통과 처리하지 않았다. 데이터 변경 검수는 별도의 브라우저 프로필과 샘플 파일로 수행한다.

| 시나리오 | 기대 결과 |
|---|---|
| 1440px 이상과 좁은 화면에서 메뉴 탐색 | 7개 주 메뉴와 더보기 고정 3개 메뉴 접근 가능, 상세에서도 활성 메뉴 표시 |
| 라이트↔다크 전환 후 새로고침 | 테마 복원, 카드·표·차트·모달의 글자 가독성 확인 |
| 목록 검색→정렬→2페이지→상세→복귀 | 저장 대상인 검색·정렬·페이지 복원. 스크롤은 별도 확인 |
| 생산 분석 상단 탭과 표 탭을 다르게 선택 | 두 영역이 독립적으로 갱신됨 |
| 가동률 목표 변경→셀 상세→원본 이동 | 목표 반영 및 대상 날짜·설비 범위 확인 |
| 비가동 셀 선택→선택 해제 | 상세 내역이 좁혀졌다가 전체 조회 범위로 복귀 |
| 품번→작업자→복귀 | 품번 스코프와 출발 메뉴 유지 |
| 정상 샘플 업로드→새로고침 | 파일명·분석 대상·업로드 DATA 유지 |
| 파싱 불가 파일 업로드 | 오류 안내, 이전 활성 DATA 유지 |
| 정상 행 수정→분석 제외 조건으로 저장 | 관리자 로그인 후 정상 집계에서 제외되고 오류 DATA/검증 결과에서 확인 가능 |
| 비관리자 생산 DATA 수정 시도 | 수정 불가·로그인 유도. PATCH는 403 |
| 표 검색 후 Excel 다운로드 | 현재 페이지가 아닌 조회 결과 전체 수와 파일 내용 대조 |
| 시드 복원→새로고침 | 가데이터 복귀. 이전 업로드 자동 복원 안 됨 |

문서 재갱신 순서: 라우트·메뉴 → 공통 UI → 화면별 조작 → 계산·저장·Excel → 구현/목표 구분 → 기준일·변경 이력 순으로 확인한다. 문서의 권장 설계를 곧바로 구현된 기능으로 승격하지 않는다.

---

*문서 끝 — V1.3*
